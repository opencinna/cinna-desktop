---
description: Write or fix a Playwright E2E test for a user scenario, via the e2e-test-writer agent.
---

Write or fix an end-to-end test for this user scenario, using the `e2e-test-writer` agent (Agent tool, `subagent_type: e2e-test-writer`):

$ARGUMENTS

Pass the scenario to the agent verbatim. When it reports back, relay what the spec proves, the run results, and anything it flagged as a product defect or as needing input from the user.

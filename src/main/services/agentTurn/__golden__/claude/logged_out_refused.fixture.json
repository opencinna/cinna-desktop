{
  "recorded_from": "hand-written",
  "source": "claudeAgentTurnRunner.test.ts › readiness, answered before the turn rather than as a failed one › a logged-out install is refused before a turn is spawned, not after one fails",
  "description": "The login probe answers logged_out: the turn is refused before query() is called at all.",
  "deps": {
    "approval": "auto",
    "claudeAuth": "logged_out"
  },
  "queries": []
}

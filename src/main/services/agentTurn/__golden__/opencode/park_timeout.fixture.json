{
  "recorded_from": "hand-written",
  "source": [
    "localAgentTurnRunner.test.ts › rejects a still-parked question when the turn ends, so the session cannot wedge (reject endpoint)",
    "turnStream.test.ts › records a rejected question as an explicit non-answer (question.v2.rejected shape)",
    "pendingRequests.ts › register (the park timer; production is REQUEST_PARK_TIMEOUT_MS, forced to 20 ms here)"
  ],
  "description": "Nobody answers a parked question. The registry's own park timer expires (shortened through register's timeoutMs), the runner posts the no-body question reject, the engine echoes question.v2.rejected, and the agent carries on to the end of the turn.",
  "setup": { "parkTimeoutMs": 20 },
  "steps": [
    { "op": "awaitPrompt" },
    {
      "op": "push",
      "frame": {
        "type": "question.v2.asked",
        "data": {
          "sessionID": "ses_new",
          "id": "que_8",
          "questions": [{ "question": "Which?", "header": "Pick", "options": [{ "label": "A" }] }],
          "tool": { "messageID": "msg_1", "callID": "c1" }
        }
      }
    },
    { "op": "awaitRegistered", "requestId": "que_8" },
    { "op": "checkpoint", "name": "parked" },
    { "op": "awaitReleased", "requestId": "que_8" },
    { "op": "checkpoint", "name": "timed out" },
    {
      "op": "push",
      "frame": {
        "type": "question.v2.rejected",
        "data": { "sessionID": "ses_new", "requestID": "que_8" }
      }
    },
    {
      "op": "push",
      "frame": {
        "type": "session.next.text.delta",
        "data": { "sessionID": "ses_new", "assistantMessageID": "msg_1", "textID": "t1", "delta": "No answer, so I stopped there." }
      }
    },
    {
      "op": "push",
      "frame": {
        "type": "session.next.step.ended",
        "data": {
          "sessionID": "ses_new",
          "assistantMessageID": "msg_1",
          "finish": "stop",
          "cost": 0,
          "tokens": { "input": 1, "output": 1, "reasoning": 0, "cache": { "read": 0, "write": 0 } }
        }
      }
    }
  ]
}

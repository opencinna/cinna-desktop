/**
 * The built-in guest user id. Also the storage scope for all shared settings
 * (chat modes, LLM providers, MCP providers, locally-registered agents),
 * which are available regardless of which profile is currently active.
 */
export const DEFAULT_USER_ID = '__default__'

# Copy to .env and fill in. .env is git-ignored and never published.
#
# One variable per credential field, named <env_prefix><FIELD in upper case>,
# with env_prefix declared for the slot in cinna-agent.json. For example, a slot
# named "Vendor Portal" with env_prefix "VENDOR_PORTAL_" and field "token":
#
# VENDOR_PORTAL_TOKEN=

# This agent declares no credentials yet.

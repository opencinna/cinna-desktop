# CLAUDE.md

Read `AGENTS.md` in this folder first — it says how to work on this agent, and it
wins over the workshop root's `AGENTS.md`.

When you are asked to *do the agent's job* rather than change it, read
`docs/WORKFLOW_PROMPT.md` and follow it exactly, without the build context.

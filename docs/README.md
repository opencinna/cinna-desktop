# Cinna Desktop

Desktop conversations and tasks across remote agents, folder agents running on OpenCode, Claude Code or Codex, local/SSH ACP commands and Claude Managed sessions, with model and MCP tools.

## Glossary

| Term | Definition |
|------|-----------|
| **Chat** | A conversation with an LLM, persisted in SQLite with messages and model/provider binding |
| **Chat Mode** | A named preset bundling an LLM provider/model, MCP servers, and a color — used to quickly start configured chats |
| **Provider** (a.k.a. **LLM Provider**) | An LLM service (Anthropic, OpenAI, Gemini, OpenAI-compatible, Ollama) configured with an encrypted API key — or, for a **keyless** type, with a host and no key at all. **UI naming:** surfaced to users as **"AI Credentials"** (the sidebar section, page title, "Add AI Credentials" button, and the chat-mode field). "LLM Provider" remains the canonical/technical name in code and these docs; "AI Credentials" is purely the friendlier user-facing label, matching CinnaCore. The two terms refer to the same thing |
| **Managed Provider / Chat Mode** | An LLM provider / default chat mode auto-materialized from a Cinna admin-provisioned credential (`managed = true`). Profile-scoped, sync-owned, read-only (local enable/disable only). See [Account Provisioning](llm/account_provisioning/account_provisioning.md) |
| **Adapter** | Implementation of the `LLMAdapter` interface that translates between our chat system and a provider's SDK |
| **Keyless Credential** | An AI credential that authenticates with nothing — Ollama today (`KEYLESS_PROVIDER_TYPES` in `src/shared/credentials.ts`). It carries a **host** in the existing `base_url` column instead of a key, so it needed no migration. `isCredentialUsable` is the single predicate every layer must call in place of `hasApiKey && !unsupported`: main and renderer both import it, because the rule about which credentials can run must not be able to differ between the screen that offers one and the service that spends it. It says nothing about `enabled` — that is `isCredentialActive`, the `enabled && usable` conjunction under a name, for the callers that mean both — and nothing about **reachability** — whether the server is answering has a different answer every minute and belongs to the detection probe. Two write-path rules guard it: a renderer-supplied `baseUrl` is honoured **only** for keyless types (a keyed row pointed at an arbitrary URL is key exfiltration with extra steps), and a credential's `type` is immutable, since the repo preserves a stored key on update and re-typing a row would spend that key under another transport. See [Local Models & Keyless Credentials](llm/local_models/local_models.md) |
| **Ollama Detection** | The `provider:detect-ollama` probe: `GET /api/version` then `/api/tags` against `OLLAMA_HOST` and `127.0.0.1:11434`, or against one host the renderer names. It **reaches any http(s) host it is given, and that is inherent** — a user's Ollama may legitimately be a box on the LAN, so there is no host list to check against. What bounds it is the shape of the answer and the cost of asking: `{running, host, version, models, alreadyConfigured}` and nothing else, 5 s and 1.5 s ceilings, no retry, no port scan, no LAN sweep, and **no probe the user did not cause** by opening the AI Credentials screen or pressing Test. `http`/`https` only, userinfo dropped, so a pasted link cannot smuggle a credential into a stored host |
| **MCP Server** | A Model Context Protocol server (local stdio or remote HTTP) that exposes tools to the LLM |
| **MCP Connection** | A live client session to an MCP server, managed by MCPManager |
| **On-Demand MCP** | An MCP server the user `@-mentions` into a chat so its tools are available only for that chat — tracked separately from the chat mode's baseline MCP set |
| **Baseline MCP** | The chat-mode-owned MCP set of a chat (`chat_mcp_providers`). Exactly the mode's list — empty means none. Unioned with the on-demand set at stream time; rendered as locked chips (no `×`) in the composer, since it's managed on the mode |
| **Tool Call** | A model requests a tool; a main-owned ToolProvider executes MCP, an agent turn or an authorized coordinator control and returns the result to the model |
| **MessagePort** | Electron channel carrying typed run events or sequenced live-run watch envelopes; the main executor continues when a view detaches |
| **Turn Outcome** | Internal result of one main-owned execution, distinct from whole-task completion: completed, needs_input, failed, canceled or budget, with saved final/partial text. Explicit runner ownership defers task finalization; uncertain request cleanup forbids automatic progression. See [Turn Outcomes](chat/messaging/turn_completion.md) |
| **Sidebar Session Result** | Latest device-local outcome for a chat: completed, needs_input or failed remains unread until the foreground conversation loads that result; cancellation has no badge. Main activity supplies a spinner and interrupt action independently of selection. See [Sidebar Session Status](chat/session_status/session_status.md) |
| **Run Event** | One frame from a main-owned turn, optionally forwarded over a MessagePort — `RunEvent` (`src/shared/runEvents.ts`), the single vocabulary for agent and LLM chats. A nested agent's work arrives as `child`; a message the running engine took in mid-turn arrives as `user_message`; a run waiting on a human posts `needs_input` — answerable by id now (`reply`) or by the next message (`next_message`) — and, for a `reply` ask settled while the turn is open, `input_resolved`. Events are live; transcript parts remain separate, while the Inbox persists driver reply addresses, durable next-message continuations and runner-owned gates. See [Stream Event Typing](development/stream_event_typing/stream_event_typing_llm.md) |
| **safeStorage** | Electron's OS-keychain encryption used for API keys and OAuth tokens at rest |
| **Agent** | A remote service, folder engine or configured command that takes a turn through the A2A, ACP or Managed driver; ownership and transport are independent |
| **A2A** | Agent-to-Agent Protocol v1.0 — open standard for AI agent interoperability (discovery, messaging, streaming) |
| **Agent Page** | Selecting a sidebar row opens an agent-bound composer; Settings switches to configuration and Start chat restores the composer draft. Folder and external pages share ChatWorkspace. See [Agents Tab & Agent Page](agents/local_agents/agents_tab.md) |
| **Local / Remote badge** | Execution/transport location for a single selected agent: folder and stdio ACP are Local; A2A/Cinna, WebSocket ACP and Managed are Remote. Hover/focus exposes safe connection details; multi-agent badges retain routing labels. See [Chat Routing](chat/chat_routing/chat_routing.md) |
| **Theme Preference** | System, Dark or Light, stored in renderer localStorage across local profiles/windows. System follows OS changes; Dark is the missing/invalid fallback. The sidebar shortcut chooses a fixed opposite theme. See [Appearance](ui/appearance/appearance.md) |
| **Extra UI Animation** | Default-on renderer preference for stationary-text chat curtains, occasional grid pulses, input/sidebar and secondary-button border glows, and header background waves. Respects reduced motion and input interaction. See [Appearance](ui/appearance/appearance.md) |
| **Agent Sidebar Sections** | Installation-wide `showAgentSidebarSections`, default true, controls labels and spacing while preserving group order and profile visibility. See [Settings](ui/settings/settings.md) |
| **Agent Card** | A2A discovery metadata (JSON) fetched from a well-known URL, describing agent capabilities and endpoint |
| **Message Part** | A typed segment of an assistant message (`kind: 'text' \| 'thinking' \| 'tool' \| 'tool_result' \| 'command_result'`) — A2A messages may persist a structured `parts[]` list driven by the Cinna `cinna.content_kind` metadata convention. The sibling `'notice'` kind bypasses `parts[]` and lands on its own `agent_transition` row (agent-side system notice). The `'command_result'` kind carries the synchronous output of a platform slash-command and IS the assistant turn for that user message |
| **Pinned** (transcript) | Whether the chat transcript is following the bottom of a streaming reply. Only the user changes it: scrolling up unpins, scrolling back into the bottom 64 px or clicking **Jump to latest** re-pins, and an arriving chunk never re-pins on its own. Unpinning requires the view to have actually moved *up* — not merely to be far from the bottom, since content growing opens up that distance with no gesture behind it. Following is instant and never animated; a smooth scroll restarted per chunk is what made a streaming table read as the window shaking. See [Transcript Scrolling](chat/conversation_ui/scroll_following.md) |
| **Pending Message** | A message sent into a chat whose turn is still running. Main either steers it into that turn or queues it; text only, files wait for a turn of their own. See [Pending Messages](chat/pending_messages/pending_messages.md) |
| **Steering** | Taking a pending message into the running turn over the ACP steering extension (`_session/steering`): offered only by an agent that advertised `initialize._meta.steering.supported`, only while its prompt is in flight, from the agent's first streamed content for the turn, **and no tool call is running**, only for that turn's agent, and never past a message already queued. The tool-call rule exists because the Claude adapter steers at `priority: "now"` and the CLI aborts a running command to take the message. Streams as `user_message` and is saved as a user row between the assistant rows around it |
| **Queued Message** | A pending message held in main memory per profile and chat until the running turn can take it or ends. Each time a turn offers steering — once the agent has started streaming it, and whenever its last running tool call ends — the leading run of queued messages for that turn's agent is handed in as one message; whatever is left when the turn ends drains as one turn per run of consecutive messages for the same agent — the agent resolved when it was queued. Never persisted: a restart replaying a moment's intent into a chat the user has moved on from would be worse than losing it |
| **Held Queue** | A queue that does not drain because its turn was canceled, failed or ran out of budget, or its own drained start was refused. The composer takes its text back into the input rather than sending it into a conversation that just stopped |
| **@-mention** | Typing `@` in the new-chat input to open a popup for selecting references (agents, and extensible for future types) |
| **Composer Draft** | Unsent text, files, notes and new-chat selections, isolated by profile and dashboard/agent/chat surface for one renderer session. Navigation restores it with the caret at the end; dispatch consumes only unchanged submitted fields. See [Conversation UI](chat/conversation_ui/conversation_ui.md#leaving-and-returning-to-a-draft) |
| **Message Context Menu** | Right-click Copy text or Save to Notes over a captured excerpt, or the clicked body's original Markdown when no excerpt is selected. See [Conversation UI](chat/conversation_ui/conversation_ui.md#reusing-message-text) |
| **Composer Menu** | The single left-side `[+]` button on the chat composer (`ComposerPlusMenu`) that consolidates attach-files, chat-mode, and add-agents/MCP into one menu — the mouse-driven sibling of the `@` / `~` triggers |
| **Capability** | A polymorphic chat tool source — an agent or an MCP server — surfaced uniformly as a selectable card in the `[+]` Add-agents/MCP picker |
| **Capability Picker** | The search-and-cards modal opened by the `[+]` menu's "Add agents / MCP" (`AgentPickerModal`, `activeFirst` multi-select): selected cards float to the top and hold position while toggling |
| **Router** | `chats.router` — who answers the next message: **`direct`** (the chat's bound agent, or the local model when there is none), **`human`** (several agents, and the *user* addresses one per message — **no model is involved at all**, so it works with no AI credentials configured), or **`coordinator`** (the local model conducts, calling agents and MCP servers as tools). One value read by the composer, the send path, the attachment scope and the job runner from one helper (`src/shared/chatRouting.ts`); it replaced a boolean that could not say "several agents, and the user routes". Multi-agent routing is surfaced as the badge left of Send; a single selected agent instead shows its Local/Remote execution location, independent of registration ownership. See [Chat Routing](chat/chat_routing/chat_routing.md). Only router persists after legacy orchestrated backfill and mirror removal. |
| **Address** | Which agent a message in a `human` chat is for. A **gesture, not a mention parsed from the text** — picking an agent from `@` or clicking its chip sets it; nothing scans the message body, so nothing moves while the user types. Defaults stickily to whoever the last user message addressed, then to the first attached agent |
| **Catch-up packet** | The compact transcript of what an agent missed while somebody else was answering, put in front of the user's text on the wire. Built from a per-`(chat, agent)` cursor (`chat_agent_cursors`) that advances **only on a completed turn**, so a failed or stopped turn leaves the same gap for the retry. Ordinary packets name tools and attachments without payloads; autonomous specialist packets include bounded tool-result text; capped at 4000 chars dropping the oldest. The same packet for every driver — a resumable session holds that agent's *own* turns, not the other agents' |
| **On-Demand Agent** | An agent `@-mentioned` into a chat. In a coordinated chat the conductor calls it as a tool; in a `human` chat it is one of the counterparties the user addresses. Mirrors On-Demand MCP exactly (`chat_on_demand_agents`, sticky chips, one-shot announce) |
| **Tool Provider** | Polymorphic tool source the orchestrator unions — an MCP provider (`McpToolProvider`), an agent provider (`A2AAsMcpProvider`), or internal runner-only controls (`CoordinatorToolProvider`). Tool calls route by provider type |
| **Agent Sub-thread** | An agent-backed tool call rendered as an expandable nested thread showing the agent's own parts (thinking/tool/tool_result/text) rather than an opaque result string |
| **cinna.mcp Descriptor** | Optional backend-supplied shape (tool name, description, input schema) describing how an agent appears as an emulated MCP tool. Synthesized from agent fields when absent |
| **Note Attachment** | A profile note attached to a user message via the composer's `?` mention popup. Materialized at send time into a synthetic `.md` riding the standard file-attachment pipeline |
| **Attachment** | A file attached to a single user turn. Three sources: `cinna` (bytes on the Cinna backend, A2A-referenced), `local` (bytes in `userData/files/<userId>/<chatId>/`, used for raw-LLM chats), or `pending` (composer-only, paths held on disk for the new-chat flow until scope is known) |
| **Media Part** (file) | Resolver output handed to an LLM adapter: `image`, `document` (native non-image bytes like PDF), or `text` (UTF-8 from extraction). Adapters translate to provider-native content blocks |
| **Path Guard** | TTL-based allowlist of OS paths the renderer is permitted to reference. Populated by file dialogs and `webUtils.getPathForFile` so a compromised renderer can't synthesize arbitrary absolute paths |
| **File Reference** | An inline code span, in the chat of an agent with `capabilities.cwd`, whose text main resolved to an existing file or folder. Clicking a file opens the preview modal from the click point; clicking a folder opens it in Finder. **Resolution:** per agent, over the saved transcript. A span resolves directly (absolute, `~/`, or relative to the agent folder); a short relative span may resolve against the folders of *earlier* references, but only when exactly one file matches, so a link never disappears when a later message lands. Detection only stats. Streaming bubbles never link, and resolving never probes a macOS-guarded folder, because opening a chat must not raise a privacy prompt. See [File References](chat/file_references/file_references.md) |
| **Outside-Folder Consent** | The native dialog main shows before Cinna previews, opens or reveals a file reference outside the agent's folder. Such paths are linked, and checked at the click. The renderer cannot assert consent: every read, open and reveal re-checks inside-or-approved. **Approvals** are in memory only, per profile, and shared by overlapping clicks (one dialog per path). A "don't ask again" folder approval is never offered for the home folder, anything containing it, `/` or a volume root. **Cancel** opens nothing and says nothing |
| **User Account** | A local profile (username + optional password) that scopes all data — chats, providers, agents, settings |
| **Default User** | Built-in guest account (`__default__`) with no password, always present |
| **Cinna Account** | A user account linked to a remote Cinna server (cloud or self-hosted) via OAuth 2.0 + PKCE |
| **Cinna Server** | Remote service (cloud at `opencinna.io` or self-hosted) providing agent orchestration and future features |
| **Default Scope** | Shared storage under `__default__` user id — LLM providers, MCP servers, chat modes, and local agents live here and are visible from every profile |
| **Profile Scope** | Per-account storage under the active user id — chats, remote agents, agent overrides, and Cinna tokens live here and swap on user switch |
| **Agent Override** | Per-profile boolean (`agent_overrides` table) that overlays a synced agent's `enabled` flag so user toggles survive resync |
| **Job** | Reusable profile-scoped definition. Its stored local/remote schema selects an owning Job executor; ordinary desktop work prepares one renderer turn, while autonomous and remote admission belongs to main. See [Jobs](jobs/jobs/jobs.md) |
| **Local Schedule** | Reviewed kit static-prompt declaration enabled for one profile on this device; matching observed minutes create script Job attempts while Cinna is open. Consent and occurrence receipts never sync. See [Local Schedules](jobs/tasks/local_schedules.md) |
| **Job Executor** | Main-owned definition admission, preparation and dispatch, with explicit `renderer_turn` or `accepted` result disposition. Separate from the Task’s current executor and the agent transport. See [contract](jobs/jobs/execution_tech.md) |
| **Job Folder** | User-defined sidebar grouping for jobs (profile-scoped, name + collapsed-state + sort position). Thin collapsible separator — owns ordering, not execution config. A job lives in exactly one folder or at the root |
| **Job Run** | One execution of a Job. New runs link a durable task; local attempts retain their chat and remote attempts retain their binding. The shared executor reports ordinary turn results once after closure: failure/refusal and the model-round budget become failed, Stop becomes cancelled, and open continuations defer completion. Explicit runner-owned turns leave the whole-task decision to their runner. Remote refresh projects into only the matching active attempt; historical attempts retain their outcomes. See [Jobs](jobs/jobs/jobs.md), [Tasks](jobs/tasks/tasks.md) and [Turn Outcomes](chat/messaging/turn_completion.md) |
| **Cinna Task Job** | Stored remote Job definition: creates a local task and hands it to an available adapter. Original run provenance remains; refresh follows its current task ownership |
| **Script Definition** | Version-1 portable agent aliases and dependency steps stored on a Job/Task; validated at authoring and execution. [Definition](jobs/tasks/script_definitions.md) and [runtime](jobs/tasks/script_execution.md) |
| **Autonomous Task** | Main-owned coordinator or script execution with durable questions, bounded queues and explicit checkpoint recovery; storage alone does not start work |
| **Task** | Durable, profile-scoped work with an immutable goal, current status, assignee and handoff note; a job is its reusable spec and a run is one attempt. See [Tasks](jobs/tasks/tasks.md) |
| **Inbox** | Opened from the permanent top-bar button between the sidebar toggle and New Chat. One list of live driver replies, durable runner/next-message questions and remote asks on known blocked tasks, rendered through the transcript's shared controls. Failed reads disclose an unknown count; failed delivery preserves the question draft. See [the Inbox](jobs/tasks/inbox.md) |
| **Executor** | Where a task runs now (`desktop` or `remote`), distinct from its immutable local/remote origin. A desktop executor also has a device claim; takeover changes ownership without starting work. See [Tasks](jobs/tasks/tasks.md) |
| **Task Comment** | Authored content on a cinna task (`comment_type = message \| result`); see also Activity for system-generated entries |
| **Task Attachment** | A file attached to a cinna task or one of its comments. Distinct from `FileUpload` (chat attachments) — uses a task-scoped download URL (`/api/v1/tasks/{taskId}/attachments/{id}/download`) |
| **Task Activity** | UI grouping for system-generated comment types (`status_change`, `assignment`, `system`) — shown as a compact collapsible log separate from authored comments |
| **Handoff Note** | The markdown one worker on a task leaves for the next (`tasks.handoff_note`). Also **exported** as `<userData>/tasks/<task-id>.md` — frontmatter naming the task, the note as the body — so something outside the app can read it. The database is the note and the file is a view of it: nothing is ever read back in, so the export hangs off **every** `taskService` write rather than the note write alone, because five of the frontmatter fields change from elsewhere and a file claiming `in_progress` under a task that finished an hour ago would never be corrected by anything. It never throws — the row is already committed, and a visible wrong status is worse than an invisible missing file. Not in an agent folder: exactly one file in one of those is the desktop's, and most assignees have no folder at all. The same rule reaches the sync apply path — a task arriving from another device is written through `taskService`, never `taskRepo`, so the file follows a row this machine did not author. See [The Handoff Note, Exported](jobs/tasks/handoff_note_export.md) |
| **Manifest Handback** | A declared kit agent’s bounded final-answer note, authorized only for the existing task coordinator’s handed-off specialist. Normal unmarked return remains unchanged. See [Manifest Handback](jobs/tasks/manifest_handback.md) |
| **Handoff Receipt** | Local durable record of a remote handoff attempt, including accepted or uncertain execution and explicit recovery. Survives task deletion, never syncs, and is removed with its owning account. See [Remote handoff](jobs/tasks/remote_handoff.md) |
| **Remote Task Adapter** | `RemoteTaskAdapter` — everything system-specific about a task that also lives somewhere else (cinna-core, Linear, GitHub Issues, a managed-agent service), behind one interface, the way `AgentDriver` does for agents. **Callers ask `capabilities()`, never the id**, so a poorer remote is expressible rather than broken; **only `create` invents a binding**, and every other call returns the one it was given; and **ordinary sync follows a committed local write**. Explicit handoff records uncertain create/execute outcomes before permitting another start; it does not retry them as ordinary field writes. `adapterFor(id)` is total — an id this build does not implement resolves to a **null adapter** that keeps the id, refuses everything and says which service it stands for, because a task bound by a newer build must still open. No adapter id may be compared to a literal outside `src/main/tasks/adapters/`, which the kind-branch ratchet holds at zero. Liveness is the separate tri-state `liveSession` method, never a snapshot field; an unknown answer requires an explicit take-over confirmation. One implementation ships — cinna-core — and it is a subject of the same contract suite as the six fakes, over a fake *server* rather than a fake adapter. See [Remote Task Adapters](jobs/tasks/remote_adapters.md) and [the cinna adapter](jobs/tasks/cinna_adapter.md) |
| **Remote Dirty Marker** | One entry in `tasks.remote_dirty` — `title`, `description`, `priority`, `assignee`, `status` or `handoffNote` — meaning **this device knows something the bound service does not**. Written by `taskService` on any mutation of a bound task, never by anything that talks to a network, which is what lets a laptop that was asleep, offline or unlinked push the change later. Cleared by the push, and **only when the value it stands for has not moved since the attempt**: clearing by name alone erased the marker for a field the user changed *after* the old value went up, leaving it no longer dirty so the next pull overwrote the newer value with the older one the service was given. A pull never overwrites an owed field for the same reason. `goal` is deliberately absent (immutable once created) and so is `router` — no remote has a field for who answers in this app's chat. It is also one of the two columns that **never travel to the user's other devices** (with `remote_synced_at`): a peer that has never spoken to that service must not inherit a list of fields this machine owes it. See [Keeping a Bound Task in Step](jobs/tasks/remote_sync.md) |
| **Executor Claim** | `tasks.executor_device` — the sync device id of the one device allowed to speak for a task's **run**. `status`, `assignee` and `handoffNote` are writable only there; `title`, `description`, `priority` and `router` are writable anywhere, because fixing a title on the machine in front of you is not a claim on the run. **A claim, not a lock**: nothing on the server arbitrates, so what it guarantees is only that two devices cannot both *believe* they own a run without one of them having written that it did. Enforced twice — `requireRunsHere` on the local write path and its twin on the **apply** path, since app-sync is whole-record last-writer-wins and an ungated edit on a second device otherwise pushed that device's stale copy of the three guarded fields back over the holder's fresh handoff note, finish time and error. The guard is narrow on purpose: it holds only while this device is the named holder *and* the arriving record still agrees, so a record naming a different holder is a take-over and yields entirely, and a `null` claim confers nothing. A null used to mean both "nobody in particular" and "this device", which read as *mine* on every machine once a profile gained peers — enrolment adopts what nobody holds. Surfaced as `TaskDto.runsHere`, derived in main because the renderer cannot ask which device it is. See [A Task on the User's Other Devices](jobs/tasks/cross_device.md) |
| **Status Path** | The *sequence* of status writes that moves a remote copy to where the local task is, not the destination on its own (`taskStatusPath`). A remote validates transitions, so a desktop job that ran and finished between two polls pushes `new → completed` — which is refused, on the same HTTP status that also means "not your task", which is why the most predictable defect in this area would have read as a permanent deletion. Breadth-first, because a **longer route records a false history rather than a richer one**: routing a task cancelled before it ever ran through `in_progress` makes the service stamp an execution time for work nobody did, and replaying a `blocked` that is already over lights the user's own inbox for a resolved block. Every step is itself a push, so the walk is confined to `REMOTE_WRITABLE_STATUSES` — which is what keeps `archived`, reachable from everywhere and reaching nothing, out of the middle of a path. `null` is a real answer: there is no way to tell a completed remote the task is in progress again, and the marker is dropped rather than retried for ever |
| **Note** | A profile-scoped markdown document (title + body) the user inline-edits in the Notes tab. Autosaves on debounce; rendered with the same `react-markdown` stack used for chat bubbles |
| **Note Folder** | User-defined sidebar grouping for notes (profile-scoped, name + collapsed-state + sort position). Mirrors Job Folder semantics — a thin collapsible separator that owns ordering, not content |
| **Hint** | One line of shortcut-teaching copy shown in the new-chat screen's bottom bar. Either **ambient** (enters a shuffled rotation) or **contextual** (preempts the rotation when a matching user action fires). Gated on being actionable, and retired once the user has used what it teaches. See [Hints](ui/hints/hints.md) |
| **Login-Shell Environment** | The environment a user's shell produces after sourcing its profile. A Dock-launched macOS app inherits `launchd`'s bare environment instead, so the app resolves the login shell's once per lifetime and takes `PATH` from it. See [Shell Environment Resolution](development/shell_environment/shell_environment.md) |
| **Child Inherit Set** | The fixed environment allowlist for stdio MCP and local agent children, valued from the login shell plus session and process-owned proxy/CA variables. Launcher-owned additions are explicit; Codex preserves `CODEX_HOME` for its CLI profile without inheriting shell API keys or adapter overrides |
| **Detected Tool** | A developer tool the desktop probes for on the login-shell `PATH` (`claude`, `codex`, `opencode`, `code`, `cursor`, `uv`, `git`, `make`, `python3`), offered as a `cli-assistant`, an `editor`, or a feature-gating `runtime` |
| **Default Tool** | The one `cli-assistant` or `editor` a local agent folder opens in with a single click — the agent page's **Open in <tool>** button and the New-agent flow's "Build it with…" step. Stored as a `LocalToolId` in the `localAgentsDefaultTool` setting, **rewritten by the last pick** from either surface, set or cleared explicitly in Settings → Default → Agents, and resolved against the detected list on every read so an uninstalled tool degrades to "ask". `localAgentsAutoOpen` lets a new agent open there without the step. See [Open in Tools](agents/local_agents/open_in_tools.md) |
| **Init Prompt** | The paste-anywhere briefing for one local agent folder — its path, the agent's name, and the first entry document the folder actually has (`AGENTS.md` → `CLAUDE.md` → `README.md`, else the manifest plus `Local/<slug>/docs/WORKFLOW_PROMPT.md`; a **bare** folder uses `README.md` → `AGENT.md`, the builder's document first). Built in main from the **index row**, copied to the clipboard by the Open-in menu's "Copy prompt for another tool". It is how an assistant the desktop cannot *launch* — one already running elsewhere, one in a browser, one with no launcher — is pointed at the same folder. See [Open in Tools](agents/local_agents/open_in_tools.md) |
| **Agents Root** | A registered folder local agent folders live under. Two kinds (`agent_roots.kind`): **workshop** — the kit shape, which may carry its own `.cinna-kit/` copy of the contract and is where new agents are scaffolded — and **external**, a folder the user pointed at that is only ever read. Every "Open in…" target must `realpath` to a directory at or under one, which is why overlap between roots is refused — see [Open in Tools](agents/local_agents/open_in_tools.md) |
| **External Root** | An `agent_roots` row with `kind = 'external'`: a folder adopted for the agents already in it. **Cinna installs nothing into it** — no templates, no `.cinna-kit/`, no `Local/`, no `app-data/`; the only file ever written there is an agent's own `AGENT.md`, and only when the user edits it on the agent page — and it is walked two levels deep for folders holding an `AGENT.md`. Registered by a two-call adopt (`local-agent:folder-pick`, then `:folder-add`, which refuses any path but the one just picked). See [Bare Agents & External Roots](agents/local_agents/bare_agents.md) |
| **Bare Agent** | A folder that is an agent because it holds an `AGENT.md` — singular, and deliberately not `AGENTS.md` — and for no other reason (`LocalAgentDto.kind = 'bare'`). No manifest, so no commands, credential slots, example prompts or publications; its system prompt is `AGENT.md` alone, and its identity is **positional** (`folder:external:<rootId>:<relPath>`) because there is no file to state a durable one. It **does** choose its own runtime, from the same "Runs with" panel a kit agent gets — but the answer is kept in that agent's Desktop State rather than in the folder, since the desktop writes nothing there; with no choice made it runs on the Default runtime. Adopted by path, never scaffolded. See [Bare Agents & External Roots](agents/local_agents/bare_agents.md) |
| **Agents Folder Update** | The git check on a registered root that is a working tree: is it behind, what are the missing commits, and one action — `merge --ff-only`. **It never resolves a conflict and never tries**; every state it cannot handle (`dirty`, `diverged`, `no_upstream`, `fetch_failed`, `not_fast_forward`, `git_missing`, `not_a_repo`) is a refusal naming the state and telling the user to sort it out in their own tools. Applies to workshop and external roots alike; renders nothing at all where the folder is not a repository, decided at first paint from a cheap stat walk carried on the root (`AgentRootDto.isGitRepo`) rather than from the panel's own query, so no settings row moves once the answer lands. Every command runs at the **repository root**, which may be an ancestor of the registered folder, so the panel names the repository and the refusals blame it rather than "this folder". See [Agents Folder Updates](agents/local_agents/folder_updates.md) |
| **Agents Home** | The one registered agents root marked default — `~/Documents/CinnaAgents` unless the `localAgentsHome` setting overrides it. Where the New Agent button scaffolds. Can be moved (only through the OS picker behind `local-agent:home-choose`), never removed. On macOS its **first** creation is a write into a TCC-guarded folder, so it is gated on an in-app explainer — see Home Access below |
| **Home Access** | Whether the Agents Home can be used: `ready`, `needs_consent` (nothing has touched it and macOS is about to ask), `denied` (refused). Only ever anything but `ready` on macOS. The gate lives in `agentsHomeService.ensureHome`, which **refuses** rather than writing — because the alternative was the system's *"would like to access files in your Documents folder"* prompt arriving mid sign-in, raised by a hook whose entire output was a sentence naming the folder. There is no way to ask macOS whether a prompt is pending, so the app answers from its own record: the acknowledgement in `localAgentsHomeAcknowledged`, a root row plus the folder on disk, or the path not being guarded at all. A refusal is held **in memory for the process and never stored** — macOS remembers it, a retry costs one failed `mkdir`, and a stored "denied" would keep reporting a problem already fixed. What is withheld is the **home row**, never the roots list. See [The Agents Folder Question](agents/local_agents/home_access.md) |
| **Folder Agent** | An agent that *is* a folder on disk (`agents.source = 'folder'`, row id `folder:<manifest uuid>`, protocol `local-folder`). Machine-local, default/settings scope. Distinct from `'local'` (a hand-added A2A URL) and `'remote'` (Cinna-synced). See [Agents Home, Scanner & Folder Index](agents/local_agents/folder_index.md) |
| **Folder Index** | The `agents` rows derived from scanning the roots, plus the `agent_roots` rows saying where to look. A rebuildable cache over the files, with exactly four values a rebuild cannot recover: `enabled` (the one column it owns), and a bare agent's `displayName`, `hidden` and `runtime`, which live in that agent's desktop state because its folder may not be written to |
| **Readiness** | How ready an agent folder is to run: `ok`, `credentials_needed`, `invalid`, `contract_too_new`. Always a renderable *state*, never an exception — a manifest is briefly unparseable every time an assistant saves it |
| **Turn Lock** | Per-agent in-process execution lock covering session setup and the running turn. Interactive overlap refuses; autonomous callers can queue with cancellation. Editors wait/refuse and folder watchers defer while held. Separate chat/task reservations protect admission between runner turns and human waits; a per-agent lock is not a distributed executor claim |
| **Desktop State** | One agent's per-machine state: local API base URL, agent token, engine sessions, permission grants, separate Claude/Codex **Approvals** settings, last status — plus, for a bare agent, the name the user gave it, whether it is in the list, and the runtime it was given, none of which any file in its folder can state. Two locations, and the **root** decides which: `app-data/desktop.json` inside a kit folder (the one file in it the desktop owns), and `<userData>/external-agents/<name>-<hash of realpath>.json` for a bare one, whose folder is never written into. Never probed from the folder — a `cinna-agent.json` can arrive with one `git pull`, and an agent's sessions and grants must not move when it does. The token never leaves the main process |
| **Permission Profile** | OpenCode's generated static `permission` block and optional `runtime.permissions` overrides. It allows ordinary folder work and gates broader operations, but is not an OS sandbox. Claude and Codex use their own approval mechanisms; Codex keeps a workspace-write sandbox with escalation review. See [Local Agent Permissions](agents/local_agents/permissions.md) |
| **Standing Grant** | A remembered Always allow in the agent's Desktop State, shared infrastructure with separate engine vocabularies (`bash`, `Bash`, `codex:execute`). Every resource must be covered. Codex command grants preserve the whole request scope, including network host/protocol; an engine switch cannot reuse another engine's grant. Cinna answers with one-time permission even for a saved grant, so it never creates the engine's broader persistent rules. See [Permissions](agents/local_agents/permissions.md) |
| **Grant Scope** | `exact` \| `origin` \| `action` — how widely a grant's pattern reaches, **recorded rather than inferred from its characters**. Matching is string work with no regular expression anywhere, because `rm -rf build/*` stored as a wildcard also covered `rm -rf build/../../Documents` |
| **Status Refresh Command** | `status_refresh_command` in a folder agent's manifest: how the desktop asks that agent to *recompute* its status. Only the `/run:<name>` form executes — a reference into the agent's own command catalog — and it runs under the agent's turn lock. Distinct from a **re-read**, which just reads `app-data/storage/STATUS.md` off disk. See [Agent Status](agents/agent_status/agent_status.md) |
| **Counterparty** | An agent the user picks and then expects an answer from. Every **enabled** agent is one, whatever its source: the composer `@`-list, the `[+]` capability picker and the Job agent picker all filter on `enabled` and nothing else, and `enabled` means only the user's own choice. A folder agent was excluded by a separate predicate — never by that column — until the local runner could serve one. See [Folder Agents as Counterparties](agents/local_agents/counterparty.md) |
| **Kit Contract** | The pinned, machine-readable half of the cinna-core agent start-kit bundled with the desktop (`resources/cinna-kit-contract/`): the manifest schema, the folder model (`layout.json`), and the root/agent templates. Lets the desktop scaffold, validate and export agent folders offline. See [Kit Contract](agents/local_agents/kit_contract.md) |
| **Agent Manifest** | `cinna-agent.json` at a local agent folder's root — the one file the desktop, a coding assistant and cinna-core all agree on. Unknown keys survive a read/write round trip untouched |
| **Contract Version** | Semver recorded on both the folder and the tool, and the **compatibility gate**: newer folder major → refuse and ask the user to update the app; older major → migratable; same major → run as-is. Distinct from `kit_version`, which is informational and never a gate |
| **Content Hash** | Stable SHA-256 over the files that would travel to a Cinna instance (sorted `path\0sha256(content)` lines, no mtime or inode, symlinks excluded). Answers "has this agent changed since it was pushed to that instance?" |
| **Legacy Manifest** | A `cinna-agent.json` carrying the retired integer `schema_version` and neither `contract_version` nor `id`. Read and warned about, never rejected — the one exemption from the identity requirement, encoded in the schema, the validator and the contract CHANGELOG together |
| **File Stamp** | `{mtimeMs, size, hash}` for a file an agent page can edit. Taken by the read that produced the rendered text and handed back at save time, so a write over a file an assistant changed underneath is refused. Metadata is a pre-check; the SHA-256 decides |
| **Identity State** | Where a folder agent's row id comes from: `manifest` (a UUID in `cinna-agent.json`), `legacy` (parsed, no `id` — indexed positionally), `unresolved` (unreadable manifest — listed, never indexed), `external` (a bare folder, positional by root-relative path). A folder that loses an id collision gets a fifth, *duplicate*, list id: selectable but not indexed. `external` is deliberately not `legacy` even though both are positional: the fix offered for a legacy folder is **Stamp identity**, and a bare folder has no manifest to write a UUID into |
| **Stamp Identity** | The offered — never automatic — write of a fresh UUID `id` into a legacy manifest. Re-keys the `agents` row and repoints everything that names it, rather than letting a rescan prune and re-insert |
| **Local Agent Outcome** | `LocalAgentOutcome<T>`: a failure **code** returned as data instead of thrown, because `ipcMain.handle` and `contextBridge` each drop an error's own properties. Used by `local-agent:get` and `:update-field`, whose codes drive renderer behaviour. See [Agents Tab & Agent Page](agents/local_agents/agents_tab.md) |
| **Tray Popup** | Menu-bar (status-bar) popover window listing agent statuses; opens from the macOS menu-bar tray icon, shows a worst-severity dot, and redirects the main window on click |
| **Engine** (a.k.a. **Local Engine**) | What runs a folder agent: `opencode`, `claude` or `codex` (`AgentEngine`). The folder runtime selects a launcher within the one ACP driver; without an explicit engine, runtime precedence preserves a declared credential/model on OpenCode or follows the machine default. Each agent gets an on-demand child process, reaped after two minutes idle. See [The Local Engine](agents/local_agents/engine.md) |
| **Codex Engine** | A kit/bare folder agent running the installed Codex CLI through the pinned ACP adapter. Codex owns login and user/project configuration; Cinna supplies developer instructions, optional model, reasoning effort and a workspace-write approval mode. `codexApproval` defaults to Ask for approval, separately from Claude. Questions and resumable sessions use the shared driver. See [The Codex Engine](agents/local_agents/codex_engine.md) |
| **Claude Engine** | The second engine: a folder agent running **Claude Code**, through the ACP adapter in a child process, driving the `claude` binary the user already installed under that install's own login. **The desktop is an orchestrator and never becomes an authentication provider** — no login, no stored token, no brokered session, and nothing ever read from or written to `~/.claude/`. It is one environment variable from being untrue in either direction, so the child environment is *constructed*: narrowed to the shared child allowlist (which is where `USER` comes from — without it the CLI reports "Not logged in" on a logged-in machine), then stripped by name of every key, auth token, base URL, third-party-provider switch and `CINNA_ENGINE_KEY_*`, then audited on the value actually handed over, because a turn billed to the wrong account looks exactly like a turn billed to the right one. Isolation takes three options, not one: `settingSources: []` alone left the user's own MCP connectors attached. **No `allowedTools`** is passed, since a bare name there shadows the permission request the desktop's grants run through. The adapter's own `claude` is never run — `CLAUDE_CODE_EXECUTABLE` names the user's — and `session/set_mode` is applied after every `new` and `load`, because the adapter honours a `defaultMode` from the user's own settings even under `settingSources: []`. Moving out of process is what fixed the background-subagent stdin bug the in-process SDK carried. Readiness costs nothing: `claude auth status` says whether that install is logged in without running a turn, and of what it answers the panel names the **account and the plan** while **the organisation id and name are never read out of it at all**. Contract 1.2.0's `runtime.engine`. See [The Claude Engine](agents/local_agents/claude_engine.md) and [the ACP contract](agents/local_agents/acp_contract.md) |
| **Approvals** (Claude engine) | Who answers a Claude agent's permission asks *before* the desktop does — `ClaudeApproval`, `auto` \| `ask`, per agent. **Automatic** (the default) puts Claude Code's own reviewer in front, the classifier a terminal `claude` runs with auto mode on; **Ask every time** brings every command, edit, write and fetch to the desktop's permission block. Chosen on the agent page's Permissions tab, stored in Desktop State beside the grants for either kind of folder and never in a manifest; null is *no choice made*. The reviewer was watched and **declined nothing across seven probes** — a force push, a global git config rewrite — so on Automatic the block is a backstop rather than a gate, and every surface says so. The SDK's `bypassPermissions` / `dontAsk` are deliberately unreachable. A model without a reviewer (`haiku`) runs *Ask every time* regardless, and the transcript carries a notice saying so. See [Local Agent Permissions](agents/local_agents/permissions.md) and [the contract, §10](agents/local_agents/claude_contract.md#10-auto-mode--the-classifier-in-front-of-canusetool-and-what-it-approved) |
| **Engine Config** | The OpenCode configuration generated for **one agent**, into `<userData>/acp/opencode/<hash of agent id>/opencode.json` — never into an agent folder, and written temp-file-then-rename because the engine reads it at start. It carries the provider entry for that agent's credential (an **active** one: a credential the user switched off is left out, because that switch is a statement about spending), that agent's entry with its system prompt inlined, the permission profile, and a top-level `model` — stated there *as well as* on the session, because the agent entry's own model is ignored over ACP. API keys are never in it: each is an `{env:CINNA_ENGINE_KEY_…}` reference resolved from the process environment (Invariant 4). A keyless credential names a variable too, carrying the public literal `keyless` |
| **Launch Spec** | What one agent's process was started with: command, arguments, the **whole** child environment (nothing is inherited), cwd, and a `key` that moves whenever any of those do. The pool replaces a process whose key moved before the next turn — which is how a rotated API key, an edited prompt or a changed model reaches an agent, and why a running turn keeps the process it started with. The key is a **digest, never the values**: the environment carries credentials and the key is logged |
| **Agent Key** | The OpenCode agent-entry name a folder agent becomes (`<slug>-<hash of agent id>`, always hash-suffixed so it never depends on which other agents exist). Selected on the session as the `mode` config option, which is what makes a turn run *that* agent rather than the engine's stock coding assistant in the user's folder |
| **Generated Prompt** | The per-agent system prompt assembled from the agent's own files (workflow prompt, scripts and credentials READMEs, the `knowledge/` topic *list*, handovers) plus a desktop context block. Inlined into OpenCode's config entry, passed as Claude system instructions or Codex developer instructions, and never written into the agent folder. OpenCode's v2 config reader resolves no `{file:…}` and would hand the model the placeholder instead. The readable copy the shared server used to write beside its config is gone with it |
| **Runtime** | A folder agent's engine and optional credential/model or complexity declaration. `opencode` uses desktop AI credentials; `claude` and `codex` use installed CLIs with their own login. All are ACP launchers. An explicit engine wins; a credential/model without an engine retains OpenCode; otherwise the machine default applies. Kit agents store runtime in the manifest, bare agents in Desktop State. Reading a CLI engine with a credential warns and ignores it; writes refuse it. See [The Local Engine](agents/local_agents/engine.md) |
| **Work Complexity** | `simple` \| `medium` \| `complex` in a manifest's `runtime.complexity`, in place of a model id: how hard the agent's work is, left for the host to resolve. It exists because a model id is the least portable thing `cinna-agent.json` can carry — the file is committed, read by a coding assistant and uploaded to a Cinna instance on publish, and an id means something only to the catalogue that lists it, while `medium` means the same thing on another machine, another credential and after the next model release. Resolved against the **live** catalogue by model *family* (`src/shared/modelFamilies.ts`), not a table of ids, so a new release joins its tier with no app update. A **local** model is tiered by parameter count instead of by product name — Ollama has no line-up, only whatever the user pulled, and `qwen2.5-coder:1.5b` and `:32b` are the same product in different tiers; an unsized `:latest` tag resolves to Medium rather than to nothing, because null would leave every tier empty on a machine that pulled only defaults. On the [Claude engine](agents/local_agents/claude_engine.md) it is a **small table to a plan alias** (`haiku`/`sonnet`/`opus`) instead, and deliberately not the classifier: there is no credential and no catalogue on that path, so classifying against nothing would answer null for every agent. On [Codex](agents/local_agents/codex_engine.md), complexity selects low/medium/high reasoning effort while the CLI keeps its default model. Contract 1.1.0, mutually exclusive with `runtime.model` on write. See [The Local Engine](agents/local_agents/engine.md#how-a-tier-becomes-a-model) |
| **Default Runtime** | The saved machine engine selection in Settings → Agents. An unset fresh installation prefers installed Claude, then Codex, then OpenCode; existing folder agents without a prior selection retain OpenCode. The choice is saved once, so later tooling installs do not change engines. OpenCode separately resolves its credential/model from the machine credential override or effective default chat mode, with the selected credential's Medium model floor. CLI engines use their own login/configuration. Local Development may inherit this engine while applying its independent build complexity (Complex by default) |
| **Medium Floor** | The last step of model resolution: Medium, resolved on the chosen credential, for an agent nothing above answered for. Medium and not Simple because an agent silently downgraded to the cheapest model does poor work and gives no clue why; Medium and not Complex because that failure is a bill. Never applied to the Default runtime itself — flooring twice would resolve against the *default* credential's catalogue and lend that model to an agent on a different key |
| **Inherited Model** | Which model a runtime that names none gets: the default runtime's when the credential is the default's own row → that credential's own default model (this is what covers a default chat mode left on *First available*) → the default's again when the two credentials share a provider *type*, `openai_compatible` and `ollama` excepted (two gateways, or two Ollama hosts, are two catalogues) → the **Medium floor** on that credential → nothing. One function in `src/shared/runtimeDefaults.ts` (`resolveRuntimeModel`), called by the main process **and** by the "Runs with" panel, so the `Default (…)` label and the generated config cannot name different models — an invariant three latent divergences were found under while the floor was being added |
| **Agent Turn** | One agent turn over one transport: the shared `RunAgentTurnInput` → `RunAgentTurnResult` signature, which **never throws** (a failed turn is a result carrying `error`, because an exception loses its code across IPC). Implemented by the **Agent Drivers**: A2A, ACP and Managed sessions. See [The Agent Turn](agents/local_agents/agent_turn.md) |
| **Agent Driver** | `AgentDriver` (`src/main/agents/drivers/`) — everything kind-specific about running an agent, behind one interface: `capabilities`, `readiness`, `run`, synchronous `respond` and optional `respondAsync`. There are **three** — `a2a`, `acp` and `managed` — stored on `agents.driver`. **`source` still says who owns a row; `driver` says how it runs.** `driverFor(agent)` reads only the row and serves turn dispatch and the synchronous ACP answer branch; async answers use the original registration binding. An ACP **launcher** selects a folder engine or a custom local/SSH command. Folder launchers are re-read from the folder each turn; custom launchers use captured owned configuration. This used to be a hand-off between *two folder drivers*, and while that hand-off was missing a Claude agent on a stale row answered "try again in a moment" for ever. The behavioral ratchet is **zero** across all seven categories; explicit ownership, authoring and presentation comparisons remain pinned separately. See [Agent Drivers & Readiness](agents/drivers/drivers.md). Unknown/null stored driver IDs remain visible with inert capabilities and explicit refusal; only migrations backfill legacy nulls. |
| **Agent Capabilities** | `AgentCapabilities` describes streaming, cancellation, sessions, asks/replies, attachments, auth, commands, MCP injection and cwd for each driver/launcher. Computed without I/O and carried on DTOs. Claude and Codex folder launchers support questions through ACP form elicitation; OpenCode has no question bridge. A shared transport does not imply identical engine behavior. See [Driver capabilities](agents/drivers/drivers_tech.md#capabilities-per-driver) |
| **Managed Agent** | An existing Claude workspace agent/environment bound to an Anthropic API credential and run through the `managed` driver. A private per-chat session preserves ready continuity; one-time tool confirmations wait for remote acknowledgment and durable local settlement. Distinct from an account-provisioned managed credential. See [Managed Agents](agents/managed_agents/managed_agents.md) |
| **Command-line Agent** | A locally configured ACP agent with launcher custom, started from an executable/argv array locally or through SSH. Test performs initialize only; binding-scoped sessions and remembered permissions stay in desktop state without a local agent folder. See [Command-line Agents](agents/custom_agents/custom_agents.md) |
| **Agent Readiness** | Whether an agent can take a turn **right now**, as its driver last answered: `ok`, a folder state, `not_installed` / `not_logged_in`, or `unreachable`, with a short `reason` and an optional raw `detail`. A folder's scan-time **Readiness** above is only its first rung. `agentReadinessService` keeps the answers in main's memory and merges them into the agent list; a list never waits for a check, and **`null` — never checked, or could not tell — never refuses anything**. The composer refuses a direct send to a refused agent, shows the reason and offers **Check again**, which runs fresh past every cache. A bare `/run:<name>` to a catalog agent always runs, because it is a script on this machine, not a turn. See [Agent Drivers & Readiness](agents/drivers/drivers.md) |
| **Engine Session** | An ACP session id created against a folder agent's directory, one per (chat, agent). Stored in `a2a_sessions.context_id` — the A2A-named column is reused deliberately, since that is what `agent:get-session` reads — with a durable copy in that agent's **Desktop State** — in the folder for a kit agent, under `<userData>` for a bare one. That copy is what `restoreHiddenAgents` re-seeds `a2a_sessions` from when an agent removed from the list is put back, so a bare agent's survives a removal precisely *because* it is not in the folder. Verified rather than trusted on resume: the engine's own storage can be cleared |
| **Parked Ask** (ACP) | A permission ask or question a local agent is blocked on mid-turn. Over ACP the agent is blocked on a **JSON-RPC request**, so the park *is* the unresolved response: there is no reply endpoint and no id for the agent to correlate. The desktop mints its own id (`per_acp_…` / `que_acp_…`) for the transcript and the out-of-band answer, and every exit from a turn releases what it parked — an unanswered ask is a turn that never ends |
| **Process Pool** | One ACP process per agent id (`acpProcessPool`): started lazily by the first turn that needs it, **held** for the length of a turn — idle time is the wrong signal for "in use" when a turn can be parked on a human — reaped two minutes after the last hold releases, replaced when its launch spec's key moves, and killed at app quit, as a **process group**, because an agent is rarely one process. A process that exits stays exited and is restarted by the next turn, never by a supervisor. The two minutes are measured, not guessed: an idle `opencode acp` holding a session is 311 MB of physical footprint, and a cold start is about a second |
| **Parked Request** | A permission ask or ask-user question a local agent is blocked on **mid-turn**. Rendered as a `tool` part (there is no `permission` part kind), answered out of band by request id while the turn streams, and released on every exit from the turn — an unanswered one is a turn that never ends. A release is settled as `rejected`, which is also what an expiry looks like, so the transcript distinguishes "No answer — the request expired." from "Not answered — the turn was stopped.": a decision nobody made must not be recorded as one |
| **Connect Intent** | The `cinna://connect?server=<origin>` deep link as it crosses into the renderer: an origin and when it arrived, and nothing else. Validated and reduced to an origin in main, buffered one at a time, and never acted on without the confirm step. See [The `cinna://connect` Link](auth/onboarding/connect_link.md) |
| **Confirm Step** | `ConnectIntentPanel` — the one screen a `cinna://connect` link is allowed to reach, and **the deep link's security boundary**. The URL validation is not: it narrows what a link can express, while a human reading the host is what decides whether it is acted on |
| **Local Development** | Managed tools, account setup and a one-click build-chat entry. JSON build support starts with CLI 0.4.0 and is verified through successful help probes; Check again rereads the managed executable. Default tools offers server-pinned CLI Update; Profile owns account setup and consent. See [Local Development](agents/local_dev/local_dev.md) |
| **Build Session** | A direct chat with an internal local builder bound to profile/workspace/engine. Build-specific runtime and complexity settings default to the local-agent engine and Complex; the guide exposes the actual public CLI context. Its Local tooltip names the saved builder engine without requesting private command configuration. See [Account Build Sessions](agents/local_dev/build_sessions.md) |
| **Managed Toolchain** | uv, Mutagen and cinna-cli installed into `<userData>/localdev/`. uv/Mutagen use versioned directories; CLI updates replace the stable launcher and uv environment in place. Terminal PATH integration is separately opt-in |
| **CLI Compatibility** | The managed executable's successfully probed JSON/set-token support, independent of a shell CLI or installation stamp. Failed checks are errors, never legacy evidence; Update follows the connected server's advertised version without answering setup consent. See [Local Development](agents/local_dev/local_dev.md#the-desktop-asks-the-cinna-cli-it-was-given-what-it-can-do) |
| **Account Workspace** | `<AgentsHome>/Cloud/<host>/` — a cinna-cli-owned directory holding the account token and context package for one Cinna instance. Setup checks `.cinna/account.json` for presence; the build guide reads a bounded allowlist of public Markdown documents, never the account token |
| **Setup Token** | The single-use, fifteen-minute setup command the desktop mints with its own OAuth bearer (`POST /api/v1/cli/account/setup-tokens`) and hands to cinna-cli. It is what removes the second browser login. **Argv-only** — never logged, never in an error detail, never in a state the renderer can read |
| **Local Dev Consent** | Installation-wide yes/no JSON keyed by server host in `localDevConsent`; accounts on the same host share the answer, including declines. Its controls live in Profile → Local Development without changing storage scope. Onboarding/connect flows and the later consent modal share this answer |
| **Managed Asset** | A pinned, checksum-verified install into a directory this app owns (`src/main/managed/managedAsset.ts`), shared by the local engine and the local-dev toolchain. **The presence of the install directory is the proof its bytes were verified** — everything is staged and published by one atomic rename |

## Domain Map

| Domain | Description |
|--------|-------------|
| [Core](core/resource_activation/resource_activation.md) | Cross-cutting architecture: account-scoped resources, activation gate |
| [Auth](auth/user_accounts/user_accounts.md) | Local user accounts, login/registration, user-scoped data |
| [Chat](chat/messaging/messaging.md) | Conversation CRUD, streaming, tool-call loop, [composer drafts and text actions](chat/conversation_ui/conversation_ui.md), [sidebar activity/results](chat/session_status/session_status.md), and who answers a message ([routing](chat/chat_routing/chat_routing.md)) |
| [Agents](agents/agents/agents.md) | Unified sidebar and chat-first pages for A2A, folder/custom ACP and Managed agents; discovery, readiness, lifecycle, shared local tools and [account build sessions](agents/local_dev/build_sessions.md) |
| [Jobs and Tasks](jobs/tasks/tasks.md) | Reusable job specs, reviewed local schedules, durable work records and one Inbox for local and remote asks |
| [Notes](notes/notes/notes.md) | Profile-scoped markdown notes with inline edit, chat-excerpt capture, folder organisation, and shared trash retention |
| [Sync](sync/data_sync/data_sync.md) | End-to-end-encrypted cross-device sync of profile data (notes, jobs, folders, tasks) through the zero-knowledge Cinna store |
| [LLM](llm/adapters/adapters.md) | Provider management, adapter abstraction, model selection |
| [MCP](mcp/connections/connections.md) | MCP server connections, tool aggregation, OAuth DCR |
| [UI](ui/app_shell/app_shell.md) | App shell with global agent status and Inbox, shared chat workspace, Default/Profile settings, sidebar presentation and [appearance preferences](ui/appearance/appearance.md) |
| [Development](development/setup/setup.md) | Dev environment, commands, gotchas, [release and packaged runtime verification](development/distribution/release.md) |

## Feature Registry

### Core

- [Resource Activation](core/resource_activation/resource_activation.md) — Account-scoped resource lifecycle and activation gate; every activation immediately retires former local-development work before provider reloads await
- [Settings Scope](core/settings_scope/settings_scope.md) — Default (shared) vs Profile (per-account) scope: which settings follow the user across profiles, which stay account-bound
- [Boot Resilience](core/boot_resilience/boot_resilience.md) — Fatal-startup dialog instead of ghost windows, renderer-crash detection, migration ordering safety for fresh installs

### Auth

- [User Accounts](auth/user_accounts/user_accounts.md) — Local user profiles with optional password auth, user-scoped data isolation, session management
- [Cinna Accounts](auth/cinna_accounts/cinna_accounts.md) — OAuth 2.0 + PKCE connection to remote Cinna servers (cloud or self-hosted), token rotation
- [Cinna Token Lifecycle](auth/cinna_accounts/token_lifecycle.md) — Token refresh/rotation between login and re-auth: silent near-expiry refresh, session-scoped refresh coalescing and replacement guards, and orphan-refresh safeguards (pausing autonomous background refreshes around OS suspend/resume, foreground-only run polling) that prevent an interrupted refresh from becoming a rotation-replay self-logout
- [Cinna Re-authentication](auth/cinna_accounts/reauthentication.md) — In-place re-auth when a Cinna session expires. Four entry points (the Cinna account's card in Settings → User Accounts, settings banner, agent-status overlay, chat error chip) re-run OAuth against the stored server URL, verify the OAuth-returned email matches the local user, and overwrite only the token columns — local data is preserved; local-development reconciliation runs only for the still-current, activated account
- [Onboarding](auth/onboarding/onboarding.md) — First-launch welcome screen: pick API key (validate + auto-create default chat mode) or Cinna Server (reuses self-hosted OAuth flow); force-on-restart toggle in Development settings for QA. Two later steps join the same screen: a `cinna://connect` link opens it on **`cinna-confirm`** instead of the welcome card (reachable only by arriving, never by navigating), and a connected Cinna account is followed by **`localdev`** — which skips itself when there is nothing to ask and gives up after 8 s rather than holding first run hostage
- [The `cinna://connect` Link](auth/onboarding/connect_link.md) — A landing page on a self-hosted instance hands a freshly installed desktop the one thing the download cannot carry: which server it was for. The URL is parsed as untrusted input and reduced to `url.origin` — the parse, not a substring — because `discoverCinnaEndpoints` appends `/.well-known/cinna-desktop` to whatever it is given, so a path or query would widen what a link can aim the app at for no benefit. **The confirm step is the security boundary and the validation is not**: main never opens a browser for an intent on its own, the panel renders the origin unabbreviated with no "remember this", declining stays a real always-available button (quieter, never a corner ×), and a profile that already exists turns Connect into **Switch to it** rather than switching by itself. The panel carries one decision that is about the machine rather than the link — a ticked **Enable local development** checkbox whose answer is recorded per host — which is what removes local development's separate onboarding step. One intent is buffered and a second **replaces** it (the most recent click is what the user meant); one arriving mid-OAuth is held and released by `authService`'s `finally`. Four delivery paths share one funnel — macOS `open-url` (which fires *before* `ready`, so the handler lives at module scope), `second-instance` behind the single-instance lock, an argv **scan** on cold launch, and a test-only `--cinna-connect-intent=` for the E2E suite, since Launch Services cannot be faked from Playwright. Scheme registration is skipped for the E2E sandbox profile, because claiming the machine's handler is a change to the developer's OS; an AppImage that cannot claim a scheme at all falls back to pasting the URL. Sub-doc: [Technical Details](auth/onboarding/connect_link_tech.md)

### Chat

- [Turn Outcomes and Completion Ownership](chat/messaging/turn_completion.md) — Once-only execution results, durable continuation identities, request uncertainty and explicit task-runner finalization policy; autonomous loops remain separate.
- [Messaging](chat/messaging/messaging.md) — Chat CRUD, multi-provider tool-call loop and [live run attachment/replay](chat/messaging/live_runs.md), with independent main execution and bounded replay that compacts tool output before giving up
- [Sidebar Session Status](chat/session_status/session_status.md) — Selection-independent running indicators and interruption, plus persistent unread outcomes acknowledged only after the matching foreground transcript loads. [Technical details](chat/session_status/session_status_tech.md)
- [Pending Messages](chat/pending_messages/pending_messages.md) — Sending while a turn runs: steered into a running ACP turn whose engine advertises it when the message is for that agent and no tool call is running, otherwise queued in main — handed to that turn once its running tool call ends, or drained as one turn per same-agent run once the turn completes; a stopped, failed or out-of-budget turn hands the text back to the composer. Queued bubbles can be cancelled or edited (ArrowUp recalls, Save replaces), the composer's send button is Stop on an empty input and a blue Send with text, and Esc Esc stops the turn. [Technical details](chat/pending_messages/pending_messages_tech.md)
- [Conversation UI](chat/conversation_ui/conversation_ui.md) — Message rendering (thinking open and never folded into tool dots, wrapping dot groups, a Collapse expanded pill beside Jump to latest), profile/surface composer drafts with end-caret restoration, and right-click Copy text / Save to Notes. [Technical details](chat/conversation_ui/conversation_ui_tech.md) cover state ownership, dispatch outcomes and action lifetime. Sub-doc: [Transcript Scrolling](chat/conversation_ui/scroll_following.md) — the transcript follows the bottom of a streaming reply **only while pinned**: scrolling up during a stream holds the position, an arriving chunk never drags the view back, and a "Jump to latest" pill is the way back. Following is an instant pre-paint assignment inside a `ResizeObserver`, never an animation, and unpinning needs the view to have moved up rather than merely to be far from the bottom. Fixed a chat window that "shook" while a local agent streamed a markdown table — a symptom with two independent causes, the second being a table box sized by its content, which made the whole transcript scroll sideways and cost the viewport a horizontal scrollbar's height on every chunk
- [Chat Modes](chat/chat_modes/chat_modes.md) — Named presets bundling LLM provider/model, MCP servers, and color scheme for one-click chat setup. The mode's MCP list is the chat's complete baseline: an empty list attaches **nothing** (no "all enabled providers" fallback), and switching mode on an active chat replaces the baseline verbatim
- [Example Prompts](chat/example_prompts/example_prompts.md) — Starter prompts an agent suggests, shown as an animated tag cloud on the new-chat screen and surfaced via `#` in the chat input. Two sources, one column: a remote agent's arrive from the Cinna backend sync, a folder agent's are synthesized locally from its `cinna-agent.json`
- [CLI Commands](chat/cli_commands/cli_commands.md) — `/` picker surfacing an agent's `cinna.run.*` shell commands; selecting one inserts the `/run:<slug>` invocation string
- [Mention Popups](chat/mention_popups/mention_popups.md) — Shared trigger-driven listbox primitive (`@`, `#`, `/`) backing the agent, example-prompt, and CLI-command pickers
- [Composer `[+]` Menu](chat/composer_menu/composer_menu.md) — Single left-side `[+]` button consolidating attach-files / chat-mode (sub-menu) / add-agents+MCP (search-cards modal) into one mouse-driven menu; the agent/MCP picker uses active-first ordering (selected float to top, hold on toggle) and drives the same engage/detach routing as the `@` mention. Keeps `@` / `~` / `#` / `/` / `?` as keyboard equivalents
- [Chat Routing](chat/chat_routing/chat_routing.md) — Who answers the next message, as one value on the chat row: `direct` (the bound agent, or the model), `human` (several agents, and the user addresses one per message), `coordinator` (the model conducts). The third value is the one that was missing: a second agent in a chat used to force the local model into the middle of it, so **a user with no LLM provider configured could not have two agents talk to them at all** — now they can, with the address a *gesture* rather than a mention parsed from the text, and each agent handed a catch-up packet of the thread it missed. `run:start` sends the command and `run:watch` observes the selected chat; main resolves the destination. Legacy agent/model send channels have been removed; the lower-level `run:send` port remains. Sub-doc: [Technical Details](chat/chat_routing/chat_routing_tech.md)
- [Orchestrated Agents](chat/orchestrated_agents/orchestrated_agents.md) — Agents-as-MCP: what the `coordinator` router does once a chat is on it. The local model conducts, calling each attached agent as an emulated MCP tool unioned with the real ones and rendering each as an expandable sub-thread, with agent-attributed history handoff so it understands the turns that happened before it took over. The only router that needs a model, and the only one whose switch can be refused
- [Agent Attachments](chat/agent_attachments/agent_attachments.md) — Files a remote agent attaches to its reply (`<cinna_attach>` → A2A `FilePart` with `cinna.content_kind: 'file'`). The streaming accumulator routes each to a `file`-kind `MessagePart` (deduped by `file_id`), persisted on the assistant `parts[]` and rendered inline as a downloadable badge — the mirror of user file attachments. Download goes through the Cinna OAuth bearer path (`GET /files/{id}/download`), never the signed `?token=` URI. A previewable text badge (txt, csv, md, json, yaml) opens the shared [File Preview](chat/file_preview/file_preview.md) modal instead; every other type downloads
- [File Attachments](chat/file_attachments/file_attachments.md) — `[+]` menu + drag-drop with deferred ingest on new-chat; per-model capability picks native pass-through (images, PDF on Anthropic/Gemini) vs text extraction (CSV/JSON/code via UTF-8, DOCX/XLSX/PPTX/PDF-on-OpenAI via `officeparser`); local store under `userData/files/<userId>/<chatId>/` for raw-LLM chats, Cinna backend for remote agents; path-guard allowlist + chat-ownership check + `basename` filename sanitization
- [File Preview](chat/file_preview/file_preview.md) — One read-only modal for two things: a `txt`/`csv`/`md`/`json`/`yaml` attachment badge (user or agent), and a [file reference](chat/file_references/file_references.md) in a folder agent's chat. It renders markdown, pretty-printed JSON, a CSV table with filter and sort, or wrapped text. Attachments keep Download, and other formats still download on click; agent files get Open folder and Open instead. The card expands from the click once its content settles (150 ms fallback, fade only under reduced motion). Closing plays that entrance in reverse, and the page takes clicks during the fade. It is pinned to the top so nothing it adds moves a button. It takes keyboard focus and gives it back after a keyboard open, and ignores outside presses for 500 ms after opening. Single global modal driven by `useFilePreviewStore`; attachments read through `files:read-preview`, capped at 512 KB
- [File References](chat/file_references/file_references.md) — In a folder agent's chat, inline code that names a real file or folder is clickable: a file opens the preview modal from the click point, and a folder opens in Finder. The span keeps the plain inline-code fill and gains a thin neutral inset edge, so nothing reflows when links arrive. How references resolve:
  - per agent, in main, over the saved transcript;
  - directly first, then against the folders of *earlier* references, only when exactly one file matches;
  - never while a reply is streaming, and never by probing a macOS-guarded folder.

  Main owns every decision. Paths outside the agent folder still link, but need a native consent dialog at the click; approvals are in memory and per profile. Credential files are never read into the renderer, data-volume spellings are canonicalised, and Open never executes anything. [Technical details](chat/file_references/file_references_tech.md)
- [Note Attachments](chat/note_attachments/note_attachments.md) — `?` mention popup attaches profile notes as composer badges (click to preview); at send time each note's live body is materialized as a `<safe-title>.md` routed through the existing file-attachment pipeline (`fileService.ingestSyntheticContent` → `fileService.ingest`). Double-Enter shortcut: picking a note then pressing Enter on an empty composer expands the note body inline as text instead of sending — prompt-template paste without leaving the keyboard.
- [Agent Notices](chat/agent_notices/agent_notices.md) — Agent-side system messages (`cinna.content_kind: 'notice'` TextParts) stream live as muted system pills, then collapse to a clickable accent dot once persisted as `agent_transition` rows. Excluded from LLM history rebuilds
- [Command Results](chat/command_results/command_results.md) — Synchronous platform slash-command output (`cinna.content_kind: 'command_result'` TextParts from `/files`, `/agent-status`, `/run:*` variants, …) renders as a bordered terminal-style "Command output" block. The agent stream never runs — the part IS the assistant turn and persists into the message's `parts[]` so chat previews and titles work
- [Ask User Question](chat/ask_user_question/ask_user_question.md) — Renders a remote agent's `AskUserQuestion` tool (`cinna.tool_name=askuserquestion`) as an interactive prompt instead of an opaque tool dot. An accent block with an "Answer" button opens a modal (single/multi-select options, per-question "Other" free-text, progress counter, Send gated until complete); answers are formatted to plain text and sent through the normal composer (`useChatComposer`), auto-threading onto the agent's context so it resumes. Active only on the last turn when idle — historical prompts render muted/read-only. Renderer-only: no DB column, IPC channel, or answer-status flag (state derived from message ordering)
- [Apply-Patch Diff](chat/apply_patch_diff/apply_patch_diff.md) — Renders the OpenCode/Codex `apply_patch` tool as a git-style diff (per-file op badge, path, `+N −M` tally, colorized lines) instead of a raw `patch_text` dump. A single `parsePatch()` doubles as the render guard (null → generic renderer); composes the shared `DisclosureBlock` collapsible shell extracted from the thinking/tool/result blocks
- [Auto Chat Titles](chat/auto_titles/auto_titles.md) — Opt-in background AI function: on the first user message in a new chat, generates a concise (≤ 40 char) title via the user's default chat mode LLM provider; replaces the renderer's truncated-message fallback. Fire-and-forget, fully fail-tolerant, no UI surface beyond a Settings → Features toggle

### Agents

- [Agents](agents/agents/agents.md) — A2A connections are added through Agents → Add an agent and open a chat composer first; agent-page Settings holds Overview and structured Connection details. Discovery, authentication and streaming remain driver-owned.
- [A2A Streaming Pipeline](agents/agents/streaming_pipeline.md) — Per-part delta computation, `cinna.content_kind` / `cinna.tool_name` metadata contract (text, thinking, tool, tool_result, notice, command_result), structured `parts[]` persistence
- [Remote Agents](agents/remote_agents/remote_agents.md) — Active-profile Cinna agents appear under their server domain, with reversible Desktop visibility in Profile → Agents. Agent-page actions distinguish direct-connection deletion, server deletion and consumer-bundle uninstall; hidden agents leave the sidebar and status list without stopping their server.
- [Bundles Catalog](agents/bundles_catalog/bundles_catalog.md) — Settings → Profile → Catalog: browse cinna-server agent bundles, one-click Quick Install, post-install setup modal that deep-links missing credentials to the cinna-server web pages and polls until the runtime gate clears. Sub-docs: [Inline Catalog Install](agents/bundles_catalog/inline_install.md) — a **Catalog** section in the chat composer's add-agents picker that quick-installs a bundle and auto-selects the resulting agent in one click (no Settings detour; skips the setup gate); [Bundle Updates (tech)](agents/bundles_catalog/bundle_updates_tech.md) — detecting an outdated install via the sync's `bundle_version` and applying the latest revision in-app from the Catalog card or agent-page Settings → Connection
- [Open in… (Local Agent Tools)](agents/local_agents/open_in_tools.md) — Hand a local agent's folder to the user's own tools: run Claude Code / Codex / OpenCode in a system terminal at the folder, open it in VS Code or Cursor, reveal it in the file manager, or open a bare terminal. On the agent page this is a split button whose primary is the **default tool** — a pick from its menu, or from the New-agent flow's "Build it with…" step, rewrites the default (last pick wins; Terminal and Reveal never become it), and the setting is validated against the known tool ids on write and resolved against the *installed* ones on read, so an uninstalled default degrades to "ask" rather than to a button that fails after the click. What the desktop cannot launch it **briefs** instead: **Copy prompt for another tool** puts a short, paste-anywhere instruction on the clipboard — the folder, the agent's name, and the first entry document the folder actually has — so an assistant running in another window or a browser reaches the same folder; it is built in main from the index row (never a renderer-supplied path, and never a fresh folder scan, whose name for a momentarily unparseable manifest would be the directory basename), refuses a folder no longer on disk rather than briefing a dead path, and is the one menu item that leaves the menu open, because a clipboard write changes nothing else on screen and a menu that closed would make the click look like it did nothing. Detection is one cached pass over the login-shell `PATH` (plus macOS `.app` bundles for editors, since many people never install the CLI shim), with an explicit Refresh. Every request is re-validated in main: absolute path, no control characters, `realpath`, must be a directory at or under a registered agents root on a separator boundary, and the *resolved* path is what launches. The guard is **closed by default** — with no roots provider registered every request is refused with `no_roots`, so a wiring gap fails safe. macOS terminal launches are Apple Events, so the signed build ships the automation entitlement + usage description and maps error `-1743` to `automation_denied`. Sub-doc: [Technical Details](agents/local_agents/open_in_tools_tech.md)
- [Kit Contract & Manifest Layer](agents/local_agents/kit_contract.md) — The bundled, pinned copy (contract `1.3.0`, adding optional explicit coordinator handback while retaining the open `runtime.engine` field introduced in 1.2) of the cinna-core start-kit *contract* (manifest schema, `layout.json` folder model, root/agent templates) plus the TypeScript reader, validator and exporter over it, so the desktop can scaffold, validate and export kit-shaped agent folders offline — no Python, no network. Governing principle: **files are the truth, the database is an index** — this layer never writes to SQLite. Three versions answer three separate questions (`contract_version` gates compatibility, `kit_version` is informational, `content_hash` detects drift against a publication); a legacy manifest is exempt from the identity requirement by a conditional `allOf` the schema, the validator and the CHANGELOG must change together; secret-bearing files (`credentials.json`, any `.env` but `.env.example`, `*.pem`/`*.key`/`*.p12`) are excluded at four independent layers while `credentials/README.md` and `.env.example` deliberately travel; manifest writes are content-hash guarded (mtime+size loses to `cp -p` / `rsync -t` / `git checkout`); and the hand-rolled YAML reader flags the three shapes it mis-reads and drops those command entries rather than executing a silently-truncated command. **Reading is tolerant where writing is strict**: what a newer *minor* adds is graded a warning and given defined behaviour, never an error — an error makes the scanner mark the folder `invalid` and the engine drop it entirely, which would brick a folder a future 1.x tool wrote and break the "minor bumps are additive and safe to ignore" promise the compatibility gate rests on — while this desktop still refuses to *write* either shape. Sub-doc: [Technical Details](agents/local_agents/kit_contract_tech.md)
- [Agents Home, Scanner & Folder Index](agents/local_agents/folder_index.md) — Phase 2 of Local Agents: how agent *folders on disk* become `agents` rows. The agents home (`~/Documents/CinnaAgents`) plus adopted roots, the TypeScript port of `kit.py new`, the scanner that derives readiness (`ok` / `credentials_needed` / `invalid` / `contract_too_new`) and rebuilds each root's index in one transaction, the debounced `fs.watch` per root, `app-data/desktop.json`, the per-agent turn lock, and the IPC surface over all of it. Governing rule: **a scan can only speak for the folder it walked** — it never prunes a folder it could not parse, nor rows at a location the root has moved away from, because `a2a_sessions` and `job_agents` cascade from `agents.id` and a manifest is unparseable for a moment every time an assistant saves it. An unreadable folder therefore adopts the id of the row already at its path and changes **readiness, never identity**. Watch events are a three-way `ignore` / `root` / `agent` result (collapsing them made `app-data/` — the one directory deliberately not watched — trigger a full rescan), both rescan branches honour the turn lock, and rescans must be idempotent because macOS FSEvents replays pre-arm changes on every watcher arm. `enabled` means only the user's choice and survives every rescan — it was never borrowed to express "no runner exists yet", which is why nothing had to be turned back on when one arrived. Roots may not nest — every root widens the "Open in…" path guard — and `local-agent:root-add` takes no renderer-supplied path, only a native dialog's. A root now also has a **kind**: `scanRoot` dispatches to the external walk for an adopted folder ([Bare Agents](agents/local_agents/bare_agents.md)), and everything downstream — the DTO, the index transaction, the prune, the turn lock — is shared. Sub-doc: [Technical Details](agents/local_agents/folder_index_tech.md)
- [The Agents Folder Question](agents/local_agents/home_access.md) — Asking about `~/Documents/CinnaAgents` before macOS does: the consent gate in front of the first write into a TCC-guarded folder, the `AgentsHomeModal` explainer and its refusal follow-up, the recovery affordances in the sidebar and Settings, and why a refusal is remembered for the process and never on disk
- [Agents Tab & Agent Page](agents/local_agents/agents_tab.md) — Chat-first agent pages with shared draft-preserving composer, Settings detail tabs and compact folder-runtime summaries. Sidebar rows show names/type icons, ordered as default Local folders, active Cinna server, other roots, direct A2A, ACP and Managed; Settings → Features can flatten section headings. See [Technical Details](agents/local_agents/agents_tab_tech.md).
- [The Local Engine, Runtimes & Prompt Assembly](agents/local_agents/engine.md) — What actually runs a folder agent: a **child process per agent**, spawned by the turn that needs it and reaped two minutes after it goes idle, plus the OpenCode config generated for that one agent (`<userData>/acp/opencode/<hash>/opencode.json`), the **runtime** (engine + credential + model) each agent resolves to — from a model the manifest names, a work complexity it names instead, or the defaults below it — the per-agent system prompt assembled from the agent's own files, the permission profile, and the pinned, checksum-verified `opencode` binary behind it all. There is no shared server and nothing to start: what is left of "the engine" as UI state is whether this machine has a usable binary and where it came from
- [The ACP Engine Contract](agents/local_agents/acp_contract.md) — Per-engine evidence for ACP startup, streaming, replay, permissions, questions and cancellation. Original OpenCode/Claude live probes remain distinct from Codex actual-adapter tests with a scripted native peer; live Codex model/sandbox behavior is not claimed.
- [The Agent Turn](agents/local_agents/agent_turn.md) — How a chat message addressed to a **folder agent** becomes a streaming turn against a child process speaking ACP, at parity with a remote A2A agent: read the folder → which launcher → plan or refuse in a sentence → the turn lock → acquire the agent's process → `session/load` (replay dropped) or `session/new` → the setup calls → `session/prompt` → `RunEvent`s → a stop reason. Parked asks are blocking requests answered out of band by request id, a cancel is bounded and answers the parks first, and *Always allow* never reaches the engine
- [Command-line Agents](agents/custom_agents/custom_agents.md) — Add, test and edit a local or SSH ACP command without a folder; initialization receipts, captured sessions/grants and bounded cancellation share the ACP driver.
- [Managed Agents (Claude)](agents/managed_agents/managed_agents.md) — Connect existing Claude agents/environments from Agents without a local folder; stream authoritative output, answer one-time permissions and retain bounded Stop, budget and uncertain-session states.
- [Agent Drivers & Readiness](agents/drivers/drivers.md) — Everything kind-specific about running an agent sits behind one `AgentDriver` per kind — **three** of them, `a2a`, `acp` and `managed` — and `driverFor(agent)` is the one dispatch point. Its ACP **launcher** selects a folder engine, read each turn, or a configured custom local/SSH command. The driver also answers whether an agent can take a turn right now, which the composer refuses a send on. A2A Stop interrupts silent transport waits, keeps visible partial output and preserves the previous session checkpoint; remote cancellation is best effort. Unsupported stored driver identities remain visible and refuse execution; shared pending/golden helpers now live under drivers, and Claude launcher helpers under acp.
- [The Claude Engine](agents/local_agents/claude_engine.md) — A folder agent running on **Claude Code** instead of `opencode`: the `claude` binary the user already installed, driven by the ACP adapter in a child process under that install's own login, with no credential held here at all. The desktop is an orchestrator and never becomes an authentication provider — the child environment is constructed and audited rather than inherited — and the **Approvals** setting decides whether Claude Code's own reviewer answers first, applied with `session/set_mode` after every session because a user's own `defaultMode` wins otherwise
- [The Codex Engine](agents/local_agents/codex_engine.md) — Kit/bare folder agents through the installed Codex CLI and pinned ACP adapter: CLI-owned login/configuration, model/effort selection, sandboxed approvals, questions and resumable chat sessions. [Technical details](agents/local_agents/codex_engine_tech.md) cover wiring, packaging, security and actual-adapter test limits.
- [The Claude Engine Contract](agents/local_agents/claude_contract.md) — **What is verified about Claude Code itself**, in the same spirit as the OpenCode contract: the environment bisect that made the difference between a logged-in and a "not logged in" machine, what the isolation options actually isolate, the login states, the ~190 MB `claude` the SDK ships, and the auto-mode probes. Sections describing the in-process SDK are marked retired in place; the live transport contract is the ACP one above
- [Local Agent Permissions](agents/local_agents/permissions.md) — OpenCode static profiles, separate Claude/Codex approval modes, and shared per-agent standing grants. Codex retains its workspace-write sandbox and exact command/network request scope; Cinna answers saved grants with one-time permission so engine-global rules remain untouched. [Technical details](agents/local_agents/permissions_tech.md).
- [`/run:<name>` — Catalog Commands](agents/local_agents/commands.md) — Phase 7a of Local Agents: running a folder agent's own `docs/CLI_COMMANDS.yaml` commands as a subprocess in its folder, streamed back as one `command_result` part — the same convention a remote agent's platform slash-commands already render through. <!-- nocheck --> `/run:<name>` is intercepted in `agent_a2a.ipc.ts` for an agent whose `capabilities.commands` is `catalog`, before the agent's driver is ever reached. That is deliberately outside the drivers and `agentTurn/**`, the seam Phase 6's mutation audit hardened, because a command is not a model turn and never touches the engine. For the same reason, the composer lets a bare `/run:<name>` through to a folder agent it otherwise refuses on readiness. Runs under the per-agent turn lock as a third owner, `'command'`, alongside a model turn's `'turn'` and a page save's `'editor'` — a catalog script can write anywhere under the folder just as either of those can. The whole subprocess tree is killed on cancel or timeout, not just the shell that launched it: a real defect, found by running the code rather than reasoning about it, where a killed shell still left its already-forked grandchild running and the lock held for the timeout's full duration regardless. Also flips on the agent page's **Start chat** button — hard-coded disabled since Phase 3, never turned on when Phase 6 shipped a runner able to serve a folder-agent turn, so this closes Phase 6's debt rather than being new scope of its own — and gives the composer's pre-existing `/` command popup a folder branch so it reaches a folder agent with no change of its own, the same dead-branch-until-a-phase-adds-it shape `agentStatusService.list`'s remote-only filter had. Sub-doc: [Technical Details](agents/local_agents/commands_tech.md)
- [Bare Agents & External Roots](agents/local_agents/bare_agents.md) — Any folder holding an `AGENT.md` can be adopted as an agent, without being converted first. The folder is registered as an **external** root and **nothing is ever written into it** — no templates, no `.cinna-kit/`, no `Local/`, no `app-data/` — which is why a bare agent's name, its removed-from-the-list flag, the runtime it was given and all its machine-local state live under `<userData>` instead, at a location the *root* decides rather than a probe of the folder (a `cinna-agent.json` is one `git pull` away, and an agent's sessions must not move when it arrives). The root is walked two levels deep, never into a folder that is already an agent and never into a dependency tree, so "one agent folder" and "a repository of them" are the same walk. Identity is positional, and deliberately a different identity *state* from `legacy`, because the fix offered there — Stamp identity — needs a manifest to write into. Its prompt is `AGENT.md` and nothing else the folder contains: `README.md` is excluded because it is written for whoever *builds* the agent, and is instead the first document of the init prompt — and, for the same reason, it is read on the page's **Overview** tab, rendered and read-only and absent where the folder has none, rather than beside `Instructions` on Prompts where it read as a second thing the agent is told. Adoption is two IPC calls with the picked path recorded in main, the whole picked folder becomes the root whatever was ticked (unticked folders are *hidden*, not omitted — one state, two histories), overlap with a registered root is refused first because that is the security rule, and an adopt that indexes nothing drops the root **it** created rather than closing on an empty one. Re-picking a folder already registered is a **re-selection**, not a refusal: the ticked set becomes the whole list, so unticking removes and ticking puts back — which is what makes this the only surface where a sixteenth agent can be added after fifteen were, since ⋯ takes one out at a time and Settings puts all of them back at once. Every removal's turn lock is checked before anything is written (taking them one at a time left an agent hidden under a dialog reporting that nothing had changed), a `name` reaches only an agent being added (it is prefilled from the folder, so sending it back renamed one silently), an agent put back gets its engine sessions again, and anything leaving is confirmed by name first. A bare agent also picks its **own runtime** on the same "Runs with" panel a kit agent uses, stored in its desktop state rather than in the folder and validated by the same checks as a manifest's, because an agent adopted from the user's own repository is not a lesser agent and "which credential pays for this" is the question they most need answered. Removal asks: "remove from the list only" is the default, both routes back are named, and restoring re-seeds the engine sessions (`job_agents` cannot come back, and the dialog says so). Ten surfaces on the reused agent page asserted files this folder shape never had, which is where [UX rules 9 and 10](development/ui_guidelines/ux_rules.md) came from. Sub-doc: [Technical Details](agents/local_agents/bare_agents_tech.md)
- [Agents Folder Updates](agents/local_agents/folder_updates.md) — A registered agents folder that is a git working tree can be checked for updates and fast-forwarded from Settings → Default → Agents. `merge --ff-only` is the only update it performs and **it never resolves a conflict**: dirty, diverged, no upstream, unreachable remote and a refused merge are each reported as a named reason ending in "sort it out in your own tools, or ask an agent in this folder to". `dirty` counts tracked changes only, because a folder of agents always has untracked caches; a refusal is only stated when there is something to apply, because a repository that is up to date does not need to be told to commit. The status is read without fetching (it renders once per root) and an update re-reads *with* a fetch rather than trusting the last snapshot, then rescans the root so a pulled-in agent appears. `execFile` with no shell, bounded timeouts, the narrowed child environment so `SSH_AUTH_SOCK` survives, `GIT_TERMINAL_PROMPT=0`, and a **root id** rather than a renderer-supplied path
- [Local Agents Are Not Synced](agents/local_agents/local_only.md) — **The governing product position for anything a folder agent touches across machines.** A folder agent is a directory; the desktop's sync never carries one; a job bound to one is a **local-only job**. The `source: 'folder'` job-dependency descriptor exists so an unresolvable dependency is **visible and blocking**, not so the job runs elsewhere — the app refuses the run and the blocked-job panel deliberately offers **no repair instruction** (not "copy the folder here", which promises an unshipped workflow, and not "it will run on a device where that agent is set up", which names a device that need not exist — this state is reachable by one user on one machine who has never enabled sync). The one legitimate re-resolve is the **same machine** after a re-auth or reinstall; resolving on a peer holding the same manifest id is a **mechanism, not a supported workflow**, and the matching question is undesigned. Also names a documentation failure mode in the docs' own terms: **a sentence can be mechanically accurate and become an overclaim when someone decides what the feature is for** — no commit to notice, no symbol to grep, no test that can fail.
- [Folder Agents as Counterparties](agents/local_agents/counterparty.md) — Phase 7c of Local Agents: a folder agent becomes something the user can **pick and expect an answer from**, everywhere the app offers a choice. The temporary exclusion that kept folder agents out of the composer `@` picker and the Jobs agent picker is gone, along with both call sites — a deletion rather than a migration, because it was never expressed by clearing `enabled`. It changed **three** surfaces, not one: the third is that for a user whose only agents are folder agents the filtered list was empty, so the `@` popup and the `[+]` menu did not render **at all** — the gate did not hide rows, it removed the surface that would have shown them. `remoteMetadata` on a folder row is now **synthesized locally** from its `cinna-agent.json`, which lights up `#` prompts in the composer, the new-chat tag cloud, the hint context and the "Example tasks" clause of the description an orchestrating model reads — one field, four surfaces, three of which were visibly inert and one invisibly thinner. The column name is accurate for only half its rows now: **every sentence about it must say which kind of row it means.** A sweep of all 11 non-test read sites found no consumer testing the column for *presence*, so that is debt, not a live bug. **No `cinna_mcp` descriptor is emitted, deliberately** — the provider already prefers `fallbackDescription()`, which frames the agent for the model and then appends its description and examples, and a descriptor's `description` wins the `||`; building the thing the plan asked for would have degraded a working path. A `source: 'folder'` job-dependency descriptor keyed on the **manifest id** is now emitted, where before a folder row (both URL columns null) produced no descriptor at all, was dropped from the manifest, and let the job be reported fully set up, derive the plain-LLM pattern from an empty agent list, run, and record a **success with the agent silently absent**. **The descriptor is not a passport** — local agents are not synced and a job bound to one is local-only; it exists so an unresolvable dependency is visible and blocking (see [Local Agents Are Not Synced](agents/local_agents/local_only.md)). Of the three descriptor kinds that resolve against **this machine's** own resources — MCP, hand-added local A2A agent, folder agent — it is the **only one that does not auto-create a shell on a miss**, because a folder agent *is* a directory and a row without one would assert it exists here (a remote agent resolves against a server account instead, and never auto-created either) — which is why a missing workshop is `unavailable` (nothing to open) while a present-but-switched-off row stays `needs-setup`, and why "Set up" is gated on `localId !== null` and routed on `isFolderAgentId(localId)` to Settings → Default → **Agents**. **That decision has since been taken and the run is now blocked**: `executeLocal` recomputes the unresolvable-agent set from the manifest and throws `JobError('incomplete_setup', …)` naming the missing agents, with `JobData.incompleteSetup` disabling the Run button and marking the sidebar row before the click. A `source: 'local'` shell the user *deletes* still reproduces the same agentless success and is knowingly outside the gate. The "Stamp identity" repair (a stamp used to orphan every dependent job permanently) is **outbound only**: a peer holding the stale manifest keeps it until the job is next edited, and can send the ghost back. Sub-doc: [Technical Details](agents/local_agents/counterparty_tech.md)
- [Agent Status](agents/agent_status/agent_status.md) — Shared folder/Cinna status list for the top-bar indicator, overlay and tray, with source-owned refresh behavior and partial-failure reporting. Hidden Cinna agents are excluded using profile visibility overrides; severity indicators remain here while ordinary sidebar rows and agent headers use type icons. See [Technical Details](agents/agent_status/agent_status_tech.md).
- [Local Development](agents/local_dev/local_dev.md) — Managed tools and account setup plus [Account Build Sessions](agents/local_dev/build_sessions.md): one-click composer, inspectable CLI guide, separate runtime/complexity, profile-bound builders and safe startup restoration. Minimum-version guidance, disposable capability probes, fresh Check again and server-pinned managed CLI Update keep compatibility and setup consent explicit. Develop remains the separate eligible-remote-agent flow. [Setup tech](agents/local_dev/local_dev_tech.md) · [Build-session tech](agents/local_dev/build_sessions_tech.md).

### Jobs

- [Job execution and refresh](jobs/jobs/execution_tech.md) — owned preparation/dispatch, explicit result disposition, current-task refresh and confirmed-loss attempt settlement; historical provenance stays intact.
- [Local Schedules](jobs/tasks/local_schedules.md) — Reviewed per-profile/device kit static prompts, current-minute cron admission, durable occurrence history and generated script Jobs; [technical details](jobs/tasks/local_schedules_tech.md)
- [Script Definitions](jobs/tasks/script_definitions.md) — Portable agent aliases, validated dependency graphs/templates and Job/Task sync. [Technical contract](jobs/tasks/script_definitions_tech.md)
- [Script Execution](jobs/tasks/script_execution.md) — Main-owned Job admission, isolated child conversations, graph gates and whole-script recovery. [Technical details](jobs/tasks/script_execution_tech.md)
- [Manifest Handback](jobs/tasks/manifest_handback.md) — Exact kit declaration and successful answer note for an existing coordinator-owned specialist; [technical details](jobs/tasks/manifest_handback_tech.md).
- [Autonomous Tasks](jobs/tasks/autonomous_tasks.md) — Explicit coordinator execution with durable gates, checkpoint recovery, turn/time limits and device admission queues; [technical details](jobs/tasks/autonomous_tasks_tech.md)
- [Tasks and the Inbox](jobs/tasks/tasks.md) — Durable work records browsable under Jobs, parent/subtask navigation, local/remote ownership, desktop continuation, remote handoff/recovery and one place to answer asks; [technical details](jobs/tasks/tasks_tech.md) map the schema, IPC and remaining runtime gaps
- [Remote handoff and recovery](jobs/tasks/remote_handoff.md) — Adapter-owned destinations, durable acceptance/uncertainty and explicit recovery, shared by task pages and jobs
- [The Inbox](jobs/tasks/inbox.md) — Live replies, restart-safe A2A continuations and remote questions share controls; accepted continuations run in main while the Inbox stays open, and refusals preserve drafts. Durable driver replies share transcript/Inbox commitment; captured async claims join identical answers, never resend uncertain acceptance and retry accepted local-recording failures without another remote call
- [Jobs](jobs/jobs/jobs.md) — Reusable saved work specs (prompt + agents/mode/MCP config) with a sidebar Chats/Jobs tab strip and drag-and-drop folders for organising jobs; local runs route through the shared `newChatRouter` (one agent → bound directly, several → a chat the user routes, agents + connectors → the model coordinates) and spawn a chat already on that router, Cinna Task runs hit cinna-core and poll for status. A local run's history row opens **the task it produced** rather than that chat — the chat is hidden from the Chats list and is deleted with the run, while the task outlives both — with the conversation kept as its own labelled action beside it
- [Cinna Task Run View](jobs/cinna_task_view/cinna_task_view.md) — Read-only in-app view of a `cinna_task` run: comments + standalone/inline attachments fetched from cinna-core, markdown-rendered, with task-scoped attachment download; reached by clicking a cinna_task row in a job's run history
- [A Chat’s First Ask Creates Its Task](jobs/tasks/chat_tasks.md) — A hand-opened chat gains a task only when it raises a persisted parked ask; the full first user message becomes its goal, and the root turn finishes that task so it cannot remain running without a job completion hook
- [The Handoff Note, Exported](jobs/tasks/handoff_note_export.md) — A task's handoff note written to `<userData>/tasks/<task-id>.md` with the task's identity in YAML frontmatter: read-only for the app, never imported back, rewritten on every task write so a file that exists is current, and removed when the note is cleared or the task deleted
- [Remote Task Adapters](jobs/tasks/remote_adapters.md) — The seam for a task that also lives on another system: one interface, a capability set callers ask instead of branching on a service name, a total registry whose unknown ids resolve to a refusing null adapter, and a contract suite run over six worked fake services **and the one real adapter**, which passed its clauses without the seam moving
- [cinna-core as a Remote Task Adapter](jobs/tasks/cinna_adapter.md) — The first implementation of that seam, and the only file in the tree outside the task-view feature that names cinna's routes and field names. Four server behaviours a reasonable adapter gets wrong and each costs something: a **400 is not "not yours"** (an ownership refusal and an illegal transition share the status code, so `not_ours` needs a 404 or an exact body match — a prefix match would unbind a task over a mis-chosen assignee), `archived` is its own route, `execute` answers 200 with `success: false`, and an uncursored list is the *active* set rather than everything. `fetch` is one detail request; `liveSession` separately asks whether anything is working there when Take over needs that answer, because task status cannot say. `answerAsk` reads before it writes because the answer route reports nothing about what it replaced. The delta is paged by advancing the cursor to the last **complete** tie group — offset paging over a mutable sort key drops a row for ever — and a session's messages are paged to the end, because an unanswered ask is always the newest message
- [Keeping a Bound Task in Step](jobs/tasks/remote_sync.md) — The adapter coordinator for bound-task push, pull, reconciliation and handover in both directions. Jobs hand over their tasks and refresh them through this service; the task page probes and claims remote work through it. The active profile now pushes and pulls with five seconds between completed passes, catches up on focus/wake, and invalidates pending work on lifecycle changes. Watched task reads return SQLite immediately and refresh behind it; binding revisions discard out-of-order results. The local write always happened first, so an unlinked profile, an offline laptop and a 500 are one code path; a status goes up as a *path* rather than a destination; a pull never overwrites a field this device still owes; and **a task missing from a pull is a question, not a delete** — the obvious implementation removes every task the user ever finished, so each absence is confirmed with a fetch and only a `not_ours` drops anything
- [A Task on the User's Other Devices](jobs/tasks/cross_device.md) — The `task` app-sync collection and the claim that keeps two devices from both running one task. The work travels and the **conversation does not**, so a peer copy has no thread; `tasks.executor_device` names the one device that may write `status`, `assignee` and `handoffNote`, enforced on the local write path *and* on the apply path — because app-sync is whole-record last-writer-wins, so an ungated title edit on a second device pushed its stale copy of all three back over the holder's fresh handoff note, finish time and error. A `null` claim used to mean both "nobody" and "this device", so enrolment adopts what nobody holds. **Take over is offered where the work has stopped and withheld while it is streaming** — the claim does not stop the other device, so a mid-run claim would either leave the task `in_progress` for ever or be undone by the run that finishes first — and it claims without running, because that second press is the user's

### Notes

- [Notes](notes/notes/notes.md) — Inline-edit markdown notes, Save to Notes from transcript excerpts, folder organisation, drag-drop ordering, soft-delete to trash, and shared 30-day retention with chats

### Sync

- [Native Client Data Sync](sync/data_sync/data_sync.md) — Cross-device, end-to-end-encrypted sync of profile data (notes, note folders, jobs, job folders, tasks) through the zero-knowledge Cinna backend. UMK + device/recovery/passphrase unlock, QR/SAS pairing, LWW conflict resolution, tombstones; keys never leave the main process. Seamless onboarding: a login-time prompt (`SyncSetupModal`) enables sync on a first device (toggle ON by default) or restores on a new/wiped one; sign-out removes the local profile entirely and revokes this device (never wipes remote records — peers keep their data; the next sign-in restores from the cloud). Sub-docs: [Device Pairing](sync/data_sync/device_pairing.md) (commit-then-reveal SAS, auto-discovery, joiner/sealer flow), [Device Lifecycle & State Machine](sync/data_sync/lifecycle.md) (states, transitions, server-authoritative reconcile, disconnect/reconnect, recovery), [Crypto (LLM ref)](sync/data_sync/crypto_llm.md).

### LLM

- [Adapters](llm/adapters/adapters.md) — Custom LLM abstraction layer with Anthropic, OpenAI, Gemini and Ollama adapters. Two rules cut across all of them: **the credential row decides where a request goes and who it is billed to** — each vendor SDK fills unset options from the process environment, which the app inherits whenever it is launched from a terminal, so every adapter names those options explicitly and a test sets the variables and asserts nothing moves — and **a cancelled turn must reject**, since a stop that returns normally is saved as the assistant's answer and recorded as a successful run
- [Provider Integration](llm/adapters/provider_integration.md) — Cross-provider translation matrix (message history, tool schema, tool-call extraction, error parsing) and known per-provider quirks
- [Tool Schema Translation](llm/adapters/tool_schema_translation.md) — How an MCP tool's JSON Schema reaches each provider: verbatim on Anthropic/OpenAI, allowlist-translated on Gemini (whose `parameters` is a 22-field proto message that 400s the *entire* request on one unknown keyword — the `const` failure). Covers what's translated (`const` → `enum`, `$ref` → inlined, `oneOf` → `anyOf`, tuple `items`), what's degraded (empty containers, no-arg tools), what's lost, and how to re-derive the field list from Google's discovery document
- [Local Models & Keyless Credentials](llm/local_models/local_models.md) — Ollama as a first-class AI credential, for chat and for folder agents. **Keyless credentials** are the architecture: one shared `isCredentialUsable` in place of a hand-written `hasApiKey && !unsupported` at every call site, and a host in the existing `base_url` column instead of a key, so no migration. The adapter lists over the native `/api/tags` (only that surface returns the parameter size a local tier is made of) and streams by delegating to the OpenAI adapter on `<host>/v1`; the engine gets a custom provider entry with a public `keyless` placeholder, `{32768, 4096}` limits and no `tool_call` claim (`capabilities.tools: false` is catalogue metadata — the runner sends all 12 tools regardless, measured on the wire); the reconcile re-asks the **local** credentials only, and a refresh never shrinks a provider to nothing, which is what stopped an agent hanging for twenty minutes when Cinna started before Ollama. The detection probe reaches any host the renderer names — inherent, since an Ollama may be on the LAN — and is bounded by its answer shape, its ceilings and the fact that only a user action causes one
- [Switching an AI Credential Off](llm/adapters/credential_enablement.md) — Switching off a credential stops dependent chats and folder runtimes without changing their bindings. Confirmation names dependents; credential and chat-mode cards retain inactive explanations. Folder-agent chat pages show compact setup warnings and Settings → Runs on carries the full diagnostic; sidebar rows show names and type icons. Shared credential resolution keeps the renderer and engine aligned.
- [AI Functions](llm/ai_functions/ai_functions.md) — Shared primitive for one-shot LLM calls (Auto Chat Titles today; chat-summary and similar utilities in the future) — adapter resolution + `runSingleShot`
- [Account-Provisioned Providers & Chat Modes](llm/account_provisioning/account_provisioning.md) — "Ready on login": a Cinna admin provisions AI credentials centrally; the desktop auto-materializes read-only, profile-scoped **managed providers** + a default **managed chat mode** per credential from cinna-core's `GET /external/account-config` (decrypted key delivered to the trusted client, re-encrypted locally). Mirrors the Remote Agents sync pattern (deterministic ids, 5-min periodic, broadcast refresh); local vs account default-mode precedence is governed by the `prioritizeAccountDefaults` toggle (local wins by default); per-profile local enable/disable **and per-mode model override** on managed chat modes via `managed_overrides`; per-credential naming (appends the credential name so same-provider keys are distinguishable); chat-capable default-model resolution (prefers the server `default_model`, never seeds an embedding/tts model); on-demand live model fetch with the provider's real error surfaced; Anthropic OAuth tokens (`sk-ant-oat`) shown with a "Not supported" badge (no adapter/mode); admin-vs-own credential distinction (shield/cloud). Adds the `openai_compatible` adapter type (`base_url`)

### MCP

- [Connections](mcp/connections/connections.md) — MCP server lifecycle using the v2 client: automatic modern/legacy HTTP negotiation, legacy stdio/SSE, native public-client OAuth with scoped durable state, or static Bearer Token auth; tool errors retain their content and authorization changes require an explicit reconnect
- [On-Demand MCP](mcp/on_demand/on_demand.md) — `@-mention` MCP servers into a chat lazily so default chats don't pay token cost for tools they don't need; sticky chips + one-shot silent announce to the LLM. The chip strip and the `[+]` picker render the full active set (mode-owned baseline shown locked, on-demand removable) — the same union the stream sends to the LLM
- [Registries](mcp/registries/registries.md) — Browse public MCP catalogs from Settings, one-click Connect into the standard MCP flow; built-in adapters for `registry.modelcontextprotocol.io` and the Cinna-curated catalog
- [Cinna Official Registry](mcp/registries/cinna_official.md) — Curated catalog of recommended remote MCP servers, served as a static JSON from `opencinna.io`; add/remove servers without a desktop release

### UI

- [Appearance](ui/appearance/appearance.md) — System/Dark/Light preference, fixed sidebar shortcut and shared window/tray resolution; default-on stationary-text chat curtains and decorative motion with reduced-motion, visibility and interaction guards. Sub-doc: [Technical Details](ui/appearance/appearance_tech.md).
- [App Shell](ui/app_shell/app_shell.md) — Permanent top bar with sidebar toggle, Agent Status, Inbox and New Chat; floating sidebar and footer menus; MainArea view routing with ChatWorkspace shared by dashboard and chat-first agent pages.
- [Settings](ui/settings/settings.md) — Default Agents holds folders, Runtime and Tasks; Default Local Development holds managed/detected tools, server-pinned CLI Update and OpenCode Path. Profile Agents controls server visibility/sync; Profile Local Development holds workspace/setup and shared host consent. Grouped controls use inset dividers and compact right-column selectors.
- [Verbose Mode](ui/verbose_mode/verbose_mode.md) — Compact/verbose display toggle: message timestamps, meta popup, streaming block auto-expand, and structured tool-call headers in tool narration
- [Keyboard Shortcuts](ui/keyboard_shortcuts/keyboard_shortcuts.md) — Registry of every shortcut: global menu accelerators (⌘`), context-scoped ESC, chord patterns (double-ESC: clear the new-chat agent, or stop a running turn), message history and queued-message edit keys, and input-popup navigation
- [Hints](ui/hints/hints.md) — Rotating one-line tip bar stuck to the bottom of the new-chat screen, teaching composer shortcuts in place. Two lanes: an ambient rotation (session-stable shuffled order, ~7 s dwell scaled to line length, paused on hover / open picker / background window) and contextual hints that preempt it the moment the user does something adjacent to an undiscovered gesture (attach a note → learn the double-Enter inline expansion). Every hint is gated on being actionable right now and retires once the user has used what it teaches; contextual hints are additionally capped per session and per install. Renderer-only apart from one `app_settings` boolean — Settings → Features → Interface toggle (`showHints`) + Reset hints. Sub-doc: [Technical Details](ui/hints/hints_tech.md) — catalog schema, scheduler constants, storage split
- [Menu-Bar Tray](ui/tray/tray.md) — macOS menu-bar (status-bar) icon with a severity dot + frosted popover listing agent statuses; renderer-pushed icon, separate popup window, redirects the main window on click. Lifecycle gated by the `enableTrayIcon` setting (Settings → Features → Interface), togglable live

### Development

- [Agent Runtime Architecture and Completion Record](development/agent_runtime/agent_runtime.md) — Current driver/executor boundaries, retained compatibility names, measured phase results and explicit conditional exclusions.
- [Setup](development/setup/setup.md) — Dev commands, tech stack, gotchas, project status
- [End-to-End Tests](development/e2e/e2e.md) — Playwright drives the *built* Electron app (`npm run test:e2e`), one throwaway `HOME`+`userData` sandbox per test via the `CINNA_USER_DATA` seam, and `CINNA_BACKGROUND_WINDOW=1` so a run does not take the foreground away from the developer (accessory activation policy **plus** `showInactive` — either alone still activates the app); real UI by role, `window.api.*` for arrange steps, `electronApp.evaluate` to stub native dialogs; manual-plan items map 1:1 to specs, expected defects as `test.fail()`. The IPC error wire string is *observed* here, not derived. Born from the `{{SLUG}}` scaffold defect nothing else could see. Sub-doc: [Writing E2E Tests](development/e2e/e2e_llm.md) — how to write and debug a spec (LLM reference; the `e2e-test-writer` agent follows it)
- [UI Guidelines](development/ui_guidelines/ui_guidelines_llm.md) — Color system, the **two type scales** (settings 14/13/12 vs. app-chrome `text-xs`/`text-[10px]`/`text-[9px]`) and which surface takes which, the settings section pattern and the `SettingsLayout` primitives a settings tab is built from (Default → Agents is the first caller; the older tabs match the scale with their own markup), expandable card pattern, button layout rules, form conventions (LLM reference)
- [UX Rules](development/ui_guidelines/ux_rules.md) — How a screen behaves, each rule with the decision that produced it: nothing jumps while the user types or steps through a wizard (validate on blur/submit, reserve space, no per-keystroke hints); a page is a control surface first (≤3 primary actions, ⋯ menu for occasional ones, tabs with count badges for information, banners only when something needs attention); ask for the minimum at creation; remember the last choice with an explicit override and opt-in auto-use; destructive actions confirm, prefer the Trash, are undismissable while pending and tell busy from failed; errors close nothing and land beside the control; copy fits and sub-lines never repeat the title; a control looks like a control rather than like the prose beside it, since hover is not an affordance; and a settings tab is a stack of titled sections at the scale of the tab beside it, because every settings screen is one click from every other and a tab that is internally tidy but unlike its neighbours still reads as unfinished. Enforced by the `cinna-desktop-ux-reviewer` agent on every user-visible change
- [Logger](development/logger/logger.md) — In-app debug logger with full-window overlay, scoped loggers for main/renderer, ⌘` keyboard shortcut. **`logger/logger.ts` has zero imports, by rule** — it used to import `getMainWindow` from `src/main/index.ts` so it could broadcast to the renderer itself, so every one of the 109 files that import it (76 non-test modules under `src/main/`, counted at `12686f0`) dragged in Electron and the entry point, and a pure function's test three layers away had to stub the logger just to *load* (a file that fails to load reports as a **smaller test count**, not a failure). The broadcast is now an installed **sink**: `logger/broadcast.ts` holds the Electron half and takes the window getter **as an argument** rather than importing it, so the cycle is cut rather than moved. Redaction, the 2000-entry cap, the channel and the console write are unchanged. Known and accepted: a sink that throws on *every* entry is retried forever and reported nowhere, and the single `installLogBroadcast` call is deliberately uncovered — dropping it leaves an overlay that opens fully populated and then never updates
- [Main-Process Layering](development/main_layering/main_layering_llm.md) — `db → services → ipc` convention, `ipcHandle()` wrap, DomainError codes, DTO masking (LLM reference)
- [Database Migrations](development/migrations/migrations_llm.md) — Inline, version-less, idempotent SQLite migrations run every boot from `runMigrations()`; ordering rules, the FK-off-during-migration pattern, `hasTable`/`hasColumn` guards, and fresh-install validation (LLM reference)
- [Stream Event Typing](development/stream_event_typing/stream_event_typing_llm.md) — The one event vocabulary for agent and LLM chats: raw `RunEvent` on the lower-level send port, sequenced `RunWatchMessage` envelopes on live subscriptions, one `isRunEvent` guard at the contextBridge boundary, one `useRunEventHandler` receiver, `postRunError` for IPC pre-flight failures. Includes the `needs_input` / `input_resolved` contract for a run waiting on a human (who posts, after the ask's part, `reply` vs `next_message`, teardown posts nothing), the mapping from the retired `AgentStreamEvent` / `LlmStreamEvent` / `tool_subevent`, and why "never unify" was overturned: two unions had grown a third and two renderer handlers that drift (LLM reference)
- [Shell Environment Resolution](development/shell_environment/shell_environment.md) — Shared main-process resolver that recovers the user's login-shell environment (chiefly `PATH`), which a GUI-launched macOS app does not inherit: one probe per app lifetime behind a sentinel + `env -0` parse, never throws, falls back to `process.env`, plus a cached bare-name `which()`. **Every child spawn must source its environment here.** Also owns the child inherit rule — the MCP SDK's allowlist valued from the login shell, plus session variables and (win32) `PATHEXT`, plus proxy/CA variables taken from `process.env` only, minus `()`-valued entries — which deliberately *narrows* what a stdio MCP server can read, with a names-only debug log of what was dropped. Sub-doc: [Technical Details](development/shell_environment/shell_environment_tech.md)
- [Release & Distribution](development/distribution/release.md) — Full release cycle: macOS signing/notarization, Linux build via GitHub Actions, GitHub Releases, in-app auto-update. Aspect: [Packaged Runtime Dependencies](development/distribution/packaged_runtime.md) — ACP dependency discovery and shipped-tree build guard, per-target native preparation, isolated ACP/main smoke commands, CI coverage and platform evidence limits.
- [Auto-Update](development/auto_update/auto_update.md) — Runtime auto-update behavior: state machine, sidebar footer progress indicator, "Check for Updates…" menu, restart prompt

## Architecture

```
+------------------------------------------------+
|                 MAIN PROCESS                   |
|                                                |
|  SQLite (Drizzle)  LLM SDKs  MCP Clients  A2A |
|  User accounts (PBKDF2)  Session management    |
|  API keys & tokens encrypted via safeStorage   |
|                                                |
|  IPC Handlers (ipcMain.handle / .on)           |
|  + MessagePort for streaming                   |
+------------------+-----------------------------+
                   |  contextBridge (typed window.api)
+------------------+-----------------------------+
|              RENDERER (sandboxed)              |
|  contextIsolation: true, nodeIntegration: false|
|  React + Zustand + TanStack Query              |
|  window.api.auth.* / .chat.* / .providers.*    |
|  window.api.agents.* / .mcp.* / .llm.*        |
+------------------------------------------------+
```

**Security model**: API keys and OAuth tokens are encrypted at rest using Electron's `safeStorage` (OS keychain) and stored as blobs in SQLite. They are decrypted only in the main process. User passwords are hashed with PBKDF2-SHA512 (100k iterations). All data is user-scoped — every IPC query filters by the active user ID. The renderer is fully sandboxed — it can only access the typed `window.api.*` methods exposed via contextBridge.
